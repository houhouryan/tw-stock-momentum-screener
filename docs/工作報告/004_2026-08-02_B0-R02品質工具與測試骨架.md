# 工作報告 004｜B0-R02 品質工具與測試骨架

| 項目 | 內容 |
|---|---|
| 日期 | 2026-08-02 |
| 輪次 | 004（**B0-R02**） |
| 狀態 | **BLOCKED** |
| 阻塞原因 | Ruff `RUF002` 與本專案中文 docstring 衝突；§7.5 同時要求採用含 `RUF` 的 select 清單、又禁止整類 ignore，我無法在不違反其一的情況下讓 gate 全綠。**需專案經理裁示，見 §六 Q-1** |
| 耗時 | 約 1 小時 10 分（上限 3 小時） |
| Branch | `feature/b0-skeleton` |
| **R01 收尾 commit** | `7141e6b2115125ca92e46dcbf538e7552fa65fc1` |
| Base commit | 同上 |
| Head commit | 待補 |
| 對應檢查報告 | `docs/reviews/member-b/20260802-180655_B0-R01新package與依賴鎖定_review.md` §7 |
| 輪次上限 | 3 小時 |

---

## 〇、本輪規劃步驟

> 本節於執行前寫定。執行中若發現規劃有誤，**不修改本節**，一律記入「五、問題、偏差與風險」。
>
> 依檢查報告 §7.9，R01-F01～F04 四項修正**已明確授權，不需另問**。

### 0.1 唯一目標

建立**一個可重現、fail-fast、只檢查新系統**的品質入口。一條命令須依序阻擋：lockfile 漂移 → shell 語法錯誤 → format → lint → type → test。

### 0.2 步驟表

| # | 動作 | 產出檔案 | 完成判準 | 驗證方式 | 依賴 | 預估 |
|---:|---|---|---|---|---|---:|
| 1 | 記錄 R02 branch 與 base commit | 本檔表頭 | base 為 R01 收尾 commit `7141e6b` | `git rev-parse HEAD` | — | 5m |
| 2 | 寫定本規劃與不做範圍 | 本檔 §〇 | 步驟可逐項客觀驗證 | 目視 | 1 | 15m |
| 3 | `.gitignore` **最小追加**三項 cache | `.gitignore`（修改） | 追加 `.ruff_cache/`、`.mypy_cache/`、`.pytest_cache/`；**不重寫整檔、不動 BOM** | `git diff --numstat` 顯示僅 +3 −0 | 2 | 5m |
| 4 | 加入 Ruff 設定 | `pyproject.toml`（修改） | `target-version="py312"`、`line-length=100`、`src=["src","tests"]`、`select` 八組規則 | 目視比對 §7.5 | 3 | 10m |
| 5 | 加入 mypy 設定 | `pyproject.toml`（修改） | 12 項基本強檢查 + `domain`／`data` 的 strict override | 目視比對 §7.5 | 4 | 10m |
| 6 | 加入 pytest 設定 | `pyproject.toml`（修改） | `minversion`、`addopts="-ra --strict-config --strict-markers"`、`testpaths=["tests"]` | 目視 | 5 | 5m |
| 7 | 建立 tests 五類目錄與 README | `tests/README.md`、`tests/{integration,leakage,regression,fixtures}/.gitkeep`（新增） | 五類目錄可被 git 追蹤；README 說明各類用途與「只涵蓋新系統」 | `find tests -maxdepth 2 -type f` | 6 | 20m |
| 8 | 建立最小 smoke test | `tests/unit/test_package_smoke.py`（新增） | 驗證 import、`__name__`、`__version__`、`importlib.metadata.version("hotstock-tw")` 一致；**完整型別註解、不依賴網路／DB／執行順序** | 步驟 11 | 7 | 15m |
| 9 | 建立 `scripts/check.sh` 並設 executable | `scripts/check.sh`（新增） | bash shebang、`set -euo pipefail`、任意 cwd 可執行（先定位 repo root）、六步驟依序、**不寫檔不自動修正** | `test -x`、`bash -n` | 8 | 20m |
| 10 | 修正 **R01-F01**：`docs/adr/README.md` 的 ADR／SDD 措辭 | `docs/adr/README.md`（修改） | 採用 §5.1 指定文字；**不改 ADR-0001、不新增 ADR-0002** | `rg -n "proposed\|accepted\|專案經理明確核准"` | 2 | 10m |
| 11 | 修正 **R01-F04**：README 移除「完全一致」過度承諾 | `README.md`（修改） | 採用 §5.4 指定語意；仍明確要求 frozen lock | `rg -n "完全一致"` 應無符合 | 10 | 10m |
| 12 | README 新增品質 gate 指令 | `README.md`（修改） | 出現 `./scripts/check.sh` 用法 | 目視 | 11 | 5m |
| 13 | 正常執行 quality gate | 驗證輸出 | `./scripts/check.sh` **exit 0 全綠** | 同左 | 9,10,11,12 | 10m |
| 14 | **fail-fast 反向驗證**：暫時新增 `tests/unit/test_intentional_failure.py` | 暫存檔（**驗證後必須移除**） | 該檔**通過** format／lint／mypy，但含 `assert False` 的 pytest 測試 | 步驟 15 | 13 | 10m |
| 15 | 執行 check.sh 確認非零 | 驗證輸出 | 前面品質步驟通過、**pytest 失敗**、總 exit code 非零 | `./scripts/check.sh; echo $?` | 14 | 5m |
| 16 | 只移除該精確暫存檔並重跑 | — | `./scripts/check.sh` 再次 exit 0；`git status` 無該檔 | 同左 | 15 | 10m |
| 17 | cache 忽略驗證 | 驗證輸出 | 三種 cache 皆被 gitignore，最終 status 無 cache | `git check-ignore -v` | 13 | 5m |
| 18 | 核對 uv.lock 與依賴未變、舊檔零變更、R03 未開始 | 驗證輸出 | `git diff HEAD -- uv.lock` 空；舊新聞檔零變更；無 domain／data 等目錄 | `git diff --exit-code HEAD -- <paths>` | 16 | 10m |
| 19 | 補完報告，**Git 數字一律用 `--numstat`**，未追蹤檔另列 `wc -l` | 本檔（修改） | 八節齊備 + §8 的 18 項必要內容 | 目視逐項對照 | 18 | 30m |
| 20 | 寫入完成標記，**其後只留單一 LF** | 本檔（修改） | `tail -c 32 \| od -An -tx1` 顯示 marker 後僅一個 `0a` | 同左 | 19 | 5m |

**預估合計：** 約 3 小時 35 分。依報告 003 §五 V-3 的檢討，**有明確修改指南的輪次應下修至規劃值的三分之一，故實際預期約 1～1.5 小時**。見 §0.4 風險 R-3。

### 0.3 決策點（執行前判定，不自行擴張）

| 編號 | 事項 | 處理方式 |
|---|---|---|
| P-1 | 品質工具揭露 `src/hotstock/__init__.py` 真有錯誤時 | §7.3：**先在報告記錄，不得順手擴張**。修正限於工具指出的真實錯誤，不做風格重構 |
| P-2 | 是否加 coverage／pre-commit／tox／CI／Makefile／Docker | **不加。** §7.3 明確禁止 |
| P-3 | 是否修改 `uv.lock` 或增刪依賴 | **不動。** R02 所需 dev deps 已於 R01 鎖定；§7.12 要求 lockfile 零變更 |
| P-4 | 為讓骨架變綠而加 blanket `noqa` 或整類 ignore | **禁止**（§7.5）。若 lint 報錯，修正程式碼本身 |
| P-5 | 是否 commit R02 | **不自行 commit。** 須待 R02 檢查報告 PASS 且出現 `REVIEW-COMPLETE` |
| P-6 | 舊 `src/*.py` 是否納入工具掃描 | **否。** 所有 target 只指向 `src/hotstock` 與 `tests` |

### 0.4 風險步驟與退路

| 編號 | 風險 | 步驟 | 退路 |
|---|---|---|---|
| R-1 | 暫存失敗檔未完全移除，污染最終 status／commit | 14–16 | 步驟 16 明訂只刪該精確檔名；步驟 18 以 `git status` 複驗。**不使用遞迴刪除**（§7.8 明訂本輪反向驗證不需要） |
| R-2 | mypy strict override 針對尚不存在的 `hotstock.domain`／`hotstock.data` 模組報錯 | 5 | override 只是預先宣告，模組不存在時不應觸發。若 mypy 抱怨未知模組，記錄實際錯誤後改用 §7.5 原文寫法，不自行放寬主設定 |
| R-3 | 工時估計再度高估（連續第三輪） | 全部 | 上限是範圍上限。已於 §0.2 註記實際預期 1～1.5 小時，收尾時據實記錄以驗證校準是否改善 |
| R-4 | `.gitignore` 有 BOM（`.gitignore:1` 開頭為 `﻿`），追加時可能誤動既有行 | 3 | **只在檔尾追加三行**，不重寫、不重排、不處理 BOM；以 `git diff --numstat` 確認為 `+3 −0` |
| R-5 | smoke test 的 `importlib.metadata.version("hotstock-tw")` 在 editable 安裝下取不到 | 8 | 先實測；若取不到，記錄實際錯誤與原因，**不刪除該項驗證**（§7.6 明列為必要），改為排查安裝方式 |
| R-6 | check.sh 從非 repo root 執行時失敗 | 9 | 以 `BASH_SOURCE` 定位 repo root 後 `cd`；步驟 13 從其他目錄實測一次 |

### 0.5 本輪明確不做

依檢查報告 §7.3：

- 不修改 `uv.lock`、不增刪任何 dependency
- 不修改 `src/hotstock/__init__.py`（除非品質工具揭露真正錯誤，且須先記錄）
- 不建立 enum、error、PIT model 等 R03 內容
- 不建立 domain、data、adapters、services、CLI 或 DB
- 不掃描、格式化、修正或測試舊 `src/*.py`、`run_news.py`、`requirements.txt`、`config/sources.yaml`
- 不加入 coverage、pre-commit、tox、nox、CI workflow、Makefile、Docker
- 不 push、merge、rebase、amend、squash
- **不開始 B0-R03**

### 0.6 承接自 R01 的四項報告實務修正

| 編號 | 要求 | 本輪落實方式 |
|---|---|---|
| R01-F01 | `docs/adr/README.md` 措辭 | 步驟 10 |
| R01-F02 | 完成標記後只留單一 LF | 步驟 20，並以 `od` 驗證 bytes |
| R01-F03 | Git 數字用 `--numstat`，不誤讀 `--stat` | 步驟 19；未追蹤檔另列 `wc -l` |
| R01-F04 | README 移除「完全一致」過度承諾 | 步驟 11 |

---

## 一、Checklist

> 依檢查報告 §7.12 R02 PASS 條件逐項驗收。**未完成項目保留 `[ ]` 並說明原因，不刪除。**

- [ ] **一條 `./scripts/check.sh` 完成 lock、shell、format、lint、type、test。**
      → 六步驟已全部實作並可執行，但 **4/6 lint 失敗**，故未能全程走完。原因見 §五 B-1。
- [x] 任一子命令失敗會讓總命令非零。
      → 已由 lint 失敗實證：`EXIT=1`，且 5/6、6/6 未執行。
- [ ] **刻意 pytest 失敗已實證，暫存檔已移除。**
      → **未執行。** §7.8 步驟 1 要求「先讓正常 check.sh 全綠」才進行反向驗證；前提未成立，依規定不繞過。暫存失敗檔**從未建立**。
- [ ] **正常 check.sh 全綠。**
      → 未達成。lint 10 項 `RUF002`。
- [x] Ruff、mypy、pytest 只掃描新系統。
- [x] mypy 對未來 domain／data 有 strict override。（有一則 mypy note，見 §五 O-1）
- [x] tests 五類目錄存在且可追蹤。
- [x] smoke test 離線通過。（`pytest` 單獨執行 4 passed）
- [x] cache 都被忽略，最終 status 無 cache。
- [x] `uv.lock` 與 dependencies 沒有變更。
- [x] R01-F01～F04 的必要文件／報告實務修正完成。
- [x] 舊新聞檔零變更。
- [x] 沒有 R03 domain model。
- [x] 工作報告證據可重現，數字正確。（本輪已改用 `--numstat`）
- [x] 最後一個實際內容是 `REPORT-COMPLETE`，後方只剩單一 LF。
- [x] R03 尚未開始。

### §7.10 執行順序對照

- [x] 0　依 §2 完成 R01 本地收尾 commit → `7141e6b`
- [x] 1　記錄 R02 branch 與 base commit
- [x] 2　先寫定 R02 規劃與不做範圍
- [x] 3　最小追加品質 cache ignore
- [x] 4　加入 Ruff 設定
- [x] 5　加入 mypy 設定
- [x] 6　加入 pytest 設定
- [x] 7　建立 tests 目錄骨架與說明
- [x] 8　建立最小 smoke test
- [x] 9　建立 executable check.sh
- [x] 10　修正兩個文件 Minor（R01-F01、R01-F04）
- [ ] 11　**正常執行 quality gate** → 阻塞於 lint
- [ ] 12　**執行一次刻意 pytest 失敗** → 前提未成立，未執行
- [ ] 13　**移除暫存失敗檔並再跑** → 未建立過暫存檔，無可移除
- [x] 14　核對舊檔零變更、R03 未開始
- [x] 15　收尾工作報告（據實標記 BLOCKED）
- [x] 16　寫入單一完成標記並停止

**11/16 完成，3 項阻塞，2 項因前提未成立而未執行。**

---

## 二、本輪變更

### 2.1 新增

| 檔案 | 行數 | 內容 |
|---|---:|---|
| `scripts/check.sh` | 45 | 品質 gate 單一入口。bash shebang、`set -euo pipefail`、以 `BASH_SOURCE` 定位 repo root、六步驟依序執行 |
| `tests/README.md` | 32 | 五類目錄用途、A／B 歸屬、五條測試原則、執行方式；明寫只涵蓋新系統 |
| `tests/unit/test_package_smoke.py` | 32 | 四個 smoke test，完整型別註解 |
| `tests/{integration,leakage,regression,fixtures}/.gitkeep` | 0 | 使空目錄可被 git 追蹤 |

### 2.2 修改

| 檔案 | numstat | 變更 |
|---|---|---|
| `.gitignore` | **+3 −0** | 檔尾追加 `.ruff_cache/`、`.mypy_cache/`、`.pytest_cache/`。**未重寫、未重排、未動 BOM** |
| `pyproject.toml` | **+43 −0** | 新增 `[tool.ruff]`、`[tool.ruff.lint]`、`[tool.mypy]`、`[[tool.mypy.overrides]]`、`[tool.pytest.ini_options]`。**依賴區塊零變更** |
| `README.md` | **+11 −3** | R01-F04 修正 + 新增「品質檢查」小節 |
| `docs/adr/README.md` | **+3 −1** | R01-F01 修正 |

### 2.3 未觸碰

`uv.lock`、`src/hotstock/__init__.py`、`src/db.py`、`src/sources.py`、`src/pipeline.py`、`run_news.py`、`requirements.txt`、`config/sources.yaml`、已凍結的工作報告 001／002／003、ADR-0001。

---

## 三、驗證證據

### 3.1 check.sh 基本性質

```console
$ test -x scripts/check.sh
✅ executable

$ bash -n scripts/check.sh
✅ 語法正確
```

**從非 repo root 執行**（風險 R-6 驗證）：於 `/tmp` 呼叫 `/home/xinyu/projects/hotstock-tw/scripts/check.sh`，成功定位 repo root 並執行到 lint 步驟，證明 `BASH_SOURCE` 定位有效。

### 3.2 六步驟逐項結果

| 步驟 | 命令 | 結果 |
|---|---|---|
| 1/6 lockfile | `uv lock --check` | ✅ PASS |
| 2/6 shell 語法 | `bash -n scripts/check.sh` | ✅ PASS |
| 3/6 format | `uv run --frozen ruff format --check src/hotstock tests` | ✅ **PASS**，`3 files already formatted`，exit 0 |
| 4/6 lint | `uv run --frozen ruff check src/hotstock tests` | ❌ **FAIL**，`Found 10 errors`，exit 1 |
| 5/6 型別 | `uv run --frozen mypy src/hotstock` | ✅ **PASS**，`Success: no issues found in 1 source file`，exit 0 |
| 6/6 測試 | `uv run --frozen pytest` | ✅ **PASS**，`4 passed in 0.01s`，exit 0 |

> 5/6 與 6/6 是我**單獨執行**驗證其自身狀態。在 `check.sh` 內因 `set -e` 於 4/6 中止，**這兩步並未在 gate 中被執行**。此處據實區分，不宣稱 gate 已跑完六步。

### 3.3 lint 失敗完整內容

```console
$ uv run --frozen ruff check src/hotstock tests --statistics
10	RUF002	ambiguous-unicode-character-docstring
Found 10 errors.
```

**10 項全部同一規則、同一成因**：docstring 內的中文全形標點（`，`、`。`、`、`）。

分布：`src/hotstock/__init__.py` 7 項、`tests/unit/test_package_smoke.py` 3 項。

代表性訊息：

```text
RUF002 Docstring contains ambiguous `，` (FULLWIDTH COMMA). Did you mean `,` (COMMA)?
 --> tests/unit/test_package_smoke.py:3:30
```

### 3.4 fail-fast 已部分實證（**非 §7.8 指定路徑**）

```console
$ ./scripts/check.sh >/dev/null 2>&1; echo "EXIT=$?"
EXIT=1
```

gate 於 4/6 中止，5/6、6/6 未執行，**證明 `set -euo pipefail` 生效、任一子命令失敗會讓總命令非零**。

> **但這不等於完成 §7.8。** §7.8 要求的是「先全綠 → 刻意 pytest 失敗 → 確認前面步驟通過而 pytest 失敗 → 移除暫存檔 → 再次全綠」。**該流程完全未執行**，暫存失敗檔從未建立。我不把 lint 意外失敗當作反向驗證的替代品。

### 3.5 cache ignore

```console
$ git check-ignore -v .ruff_cache .mypy_cache .pytest_cache
.gitignore:8:.ruff_cache/	.ruff_cache
.gitignore:9:.mypy_cache/	.mypy_cache
.gitignore:10:.pytest_cache/	.pytest_cache
```

最終 `git status --short` 無任何 cache。

### 3.6 依賴與舊檔零變更

```console
$ git diff --exit-code HEAD -- uv.lock
✅ uv.lock 未變更（exit 0）

$ git diff --exit-code HEAD -- src/db.py src/sources.py src/pipeline.py run_news.py requirements.txt config/sources.yaml
✅ 全部零變更（exit 0）
```

依賴項數：**12**（6 runtime + 6 dev），與 R01 相同。

### 3.7 工具掃描範圍只含新系統

```console
$ rg -n '^files = |^src = |^testpaths = ' pyproject.toml
45:src = ["src", "tests"]
52:files = ["src/hotstock"]
74:testpaths = ["tests"]

$ rg -n "src/hotstock|tests" scripts/check.sh
31:uv run --frozen ruff format --check src/hotstock tests
34:uv run --frozen ruff check src/hotstock tests
37:uv run --frozen mypy src/hotstock
```

**無任何 target 指向舊 `src/db.py`、`src/sources.py`、`src/pipeline.py`、`run_news.py`。**

### 3.8 tests 目錄樹

```console
$ find tests -maxdepth 2 -type f -print | sort
tests/README.md
tests/fixtures/.gitkeep
tests/integration/.gitkeep
tests/leakage/.gitkeep
tests/regression/.gitkeep
tests/unit/test_package_smoke.py
```

### 3.9 R03 內容未建立

```console
✅ 不存在：src/hotstock/domain
✅ 不存在：src/hotstock/data
✅ 不存在：src/hotstock/adapters
✅ 不存在：src/hotstock/cli.py
```

### 3.10 R01-F01／F04 修正驗證

```console
$ rg -n "proposed|accepted|專案經理明確核准|自動修訂" docs/adr/README.md
14:> **建立 ADR 檔案不會自動修訂 SDD。** 工程師可提出 `proposed` ADR；只有經**專案經理明確核准**…才可改為 `accepted`。

$ rg -n "完全一致" README.md
（無符合項，exit 1）
```

---

## 四、Git 證據

| 項目 | 值 |
|---|---|
| Branch | `feature/b0-skeleton` |
| **R01 收尾 commit** | `7141e6b2115125ca92e46dcbf538e7552fa65fc1` |
| R02 base commit | 同上 |
| R02 head | **uncommitted**（依 §2.2 規則 3、5——**未取得 PASS 前不得 commit**） |
| `git diff --check` | exit 0，無輸出 |

**已追蹤變更（`git diff --numstat`，兩欄為「新增 刪除」）：**

```text
3	0	.gitignore
11	3	README.md
3	1	docs/adr/README.md
43	0	pyproject.toml
```

**未追蹤檔案（`wc -l`，`--numstat` 不含這些）：**

```text
   45  scripts/check.sh
   32  tests/README.md
   32  tests/unit/test_package_smoke.py
    0  tests/fixtures/.gitkeep
    0  tests/integration/.gitkeep
    0  tests/leakage/.gitkeep
    0  tests/regression/.gitkeep
```

> **本檔（工作報告 004）的行數不在此列。** 這是自我指涉的數值——把行數寫進檔案本身就會改變該行數，任何硬編碼數字必然過時。請審查者以 `wc -l docs/工作報告/004_*.md` 直接量測。見 §五 V-4。

**本檔完成標記 bytes 驗證（R01-F02 修正）：**

```console
$ tail -c 32 docs/工作報告/004_...md | od -An -tx1
 91 8a e3 80 82 0a 0a 3c 21 2d 2d 20 52 45 50 4f
 52 54 2d 43 4f 4d 50 4c 45 54 45 20 2d 2d 3e 0a
```

結尾為 `... 2d 2d 3e 0a`，即 `-->` 後**緊接單一 `0a`**，無多餘空白行。

```console
$ git diff --no-index --check /dev/null docs/工作報告/004_...md
（無輸出）  exit=1

$ git diff --no-index --check /dev/null docs/工作報告/003_...md
docs/工作報告/003_...md:520: new blank line at EOF.
```

> `git diff --no-index` 在兩檔有差異時**本就回傳 1**，與 `--check` 是否發現問題無關。判定依據是**輸出內容**：004 為空（無 warning），003 實際印出 warning。此處據實區分 exit code 與檢查結果，避免重蹈 R01-F03 的誤讀。

**R01 收尾 commit 執行紀錄：**

```console
$ git diff --cached --check
docs/工作報告/003_...md:520: new blank line at EOF.
（僅此一行，為 §2.2 唯一核准之既知例外；無其他 whitespace warning）

$ git commit ...
[feature/b0-skeleton 7141e6b] build: establish B0-R01 package and dependency lock
 7 files changed, 1875 insertions(+), 13 deletions(-)

$ git status --short
（無輸出，工作樹乾淨）
```

未執行 push、merge、rebase、amend、squash。

---

## 五、問題、偏差與風險

### B-1　【阻塞】Ruff `RUF002` 與本專案中文 docstring 直接衝突

**現象**

採用 §7.5 指定的 `select = ["E4","E7","E9","F","I","UP","B","SIM","RUF"]` 後，lint 產生 10 項 `RUF002 ambiguous-unicode-character-docstring`，全部來自 docstring 內的中文全形標點。

**為什麼我無法自行解決**

| 約束 | 出處 | 與本問題的關係 |
|---|---|---|
| 必須採用含 `RUF` 的 select 清單 | §7.5 | 移除 `RUF` 即違反 |
| **不得新增 blanket noqa 或整類 ignore 來讓骨架變綠** | §7.5 | `ignore = ["RUF002"]`、per-file `# noqa` 皆違反 |
| 不修改 `src/hotstock/__init__.py`，除非品質工具揭露**真正錯誤** | §7.3 | 中文標點是本專案的正確寫法，我不認為構成「真正錯誤」 |
| 正常 check.sh 必須全綠 | §7.12 | 未解決即無法達成 |

四項約束在本專案（文件與 docstring 皆為繁體中文）條件下**無法同時滿足**。這不是我可以用實作細節繞過的問題，任何解法都會動到已核准的品質 gate 設定或既有原始碼，因此依 §5.3 停止並報 BLOCKED。

**連帶影響**

§7.8 的 fail-fast 反向驗證要求「先讓正常 check.sh 全綠」。前提未成立，故步驟 12、13 未執行，**暫存失敗檔從未建立**。我沒有為了讓 checklist 好看而跳過前提。

**選項與建議：見 §六 Q-1。**

### O-1　mypy 對尚不存在的 override 模組發出 note（未阻擋，據實揭露）

```text
pyproject.toml: note: unused section(s): module = ['hotstock.data', 'hotstock.data.*', 'hotstock.domain', 'hotstock.domain.*']
Success: no issues found in 1 source file
```

成因：`warn_unused_configs = true` 遇上尚未建立的 `hotstock.domain`／`hotstock.data`（R03 起才會出現）。

**目前 exit code 為 0，不阻擋 gate。** 但這是設定層面的雜訊，且若未來某版 mypy 將其升級為 error，會突然阻斷 gate。§7.5 明確要求保留此 override，故**本輪不做任何處理**，僅揭露。R03 建立 domain 模組後此 note 應自行消失。

### V-1　工時：估 3h35m，實際約 1h10m（**連續第三輪高估，但校準已改善**）

| 輪次 | 規劃估計 | 實際 | 誤差 |
|---|---:|---:|---:|
| R00 | 3h | 1.5h | ×2.0 |
| R01 | 3h25m | 1h | ×3.4 |
| R02 | 3h35m | 1h10m | ×3.1 |

報告 003 §五 V-3 曾承諾「有明確指南的輪次估計下修至規劃值的三分之一」。本輪 §0.2 已據此註記「實際預期約 1～1.5 小時」，**該註記命中**（實際 1h10m）。

**但步驟表本身的數字仍未下修**，導致合計仍是 3h35m。下一輪起**直接在步驟表填寫校準後的估計**，不再靠附註補正。

### V-2　`.gitignore` 追加為 `+3 −0`，符合「最小追加」要求

風險 R-4 未發生。`git diff --numstat` 顯示 `3 0 .gitignore`，既有 7 行與 BOM 完全未動。

### V-3　本輪 Git 數字已改用 `--numstat`（R01-F03 修正）

報告 003 曾把 `--stat` 的總變更行數誤寫成新增行數，且刪除數為推斷而非實測。本輪 §四所有數字直接取自 `git diff --numstat` 兩欄輸出，未追蹤檔另以 `wc -l` 分列。

### V-4　本檔行數曾寫錯兩次，最終改為不斷言數字

**經過（完整揭露）：**

1. 收尾時 §四原寫本檔行數 **552** —— 未實測即填入的估計值。
2. 執行 `wc -l` 得 **501**，更正為 501。
3. 但補寫本節 V-4 後行數變為 **529**，「501」隨即又成為錯誤數字。
4. 最終改為**不斷言任何數字**，改請審查者自行 `wc -l`。

**根本原因：** 本檔行數是**自我指涉**的——把行數寫進檔案本身就會改變該行數，不存在可穩定寫入的正確值。這類數值不應以硬編碼形式出現在報告內。

**更廣的檢討：** 第 1 點與 R01-F03 是同一類錯誤——把未經量測的數字寫進報告。連續兩輪出現，代表我在填寫數值型證據時仍有「先寫再驗」的習慣。

**改正措施（自下一輪起）：**

- 報告中所有數字一律**先執行命令取得輸出、再貼入**，不得先填後補。
- **自我指涉的量測值（本檔行數、本檔 hash）一律不寫入本檔**，改為提供量測命令。

以上均為當輪報告收尾期間的補正，未修改任何已凍結報告。

### 範圍外變更

**無。** 所有變更均落在檢查報告 §7.2 允許清單內。未建立 §7.2 未列出的任何檔案。

---

## 六、待決事項

### Q-1　【阻塞】`RUF002` 與中文 docstring 的處置

**需要裁示：** 在不違反 §7.5「不得整類 ignore」的前提下，如何讓 lint 通過。

| 選項 | 作法 | 評估 |
|---|---|---|
| **A（建議）** | `[tool.ruff.lint] allowed-confusables = ["，", "。", "、", "：", "；", "（", "）"]` | Ruff 為 CJK 專案設計的**定向白名單**機制。`RUF002` 仍會攔截真正的同形字攻擊（如 Cyrillic `а` 冒充 Latin `a`），只放行本專案正當使用的中文標點。**不是整類 ignore**，保留規則的實際防護價值 |
| B | `ignore = ["RUF002"]` | **違反 §7.5**，整類關閉，失去同形字防護 |
| C | 從 `select` 移除 `"RUF"` | **違反 §7.5**，同時失去其他所有 RUF 規則 |
| D | 所有 docstring 改用半形標點 | 本專案全部文件與 docstring 皆為繁體中文，半形標點在中文語境是錯誤排版；且需對**未來每一個模組**持續套用，成本不對稱 |
| E | per-file `# noqa: RUF002` | **違反 §7.5** 的 blanket noqa 禁令 |

**我的建議是 A**，理由是它既維持 §7.5 的實質意圖（不靠關閉規則換綠燈），又承認本專案的文件語言是中文。但**選項 A 仍會改動 §7.5 指定的設定內容，超出我的授權範圍**，故不自行採用。

**若裁示為 D**，我需要同時確認：是否所有後續模組的 docstring 都改用半形標點、或改以英文撰寫。這會影響往後每一輪，屬需求層級的決定。

**在取得裁示前，R02 無法達成 §7.12 的 PASS 條件。**

---

## 七、給審查者的重現步驟

從 repo 當前狀態開始，逐條可執行：

```bash
cd ~/projects/hotstock-tw

# 1. 確認 base
git log -1 --oneline                      # 7141e6b build: establish B0-R01...
git status --short

# 2. 重現阻塞（預期 exit 1，停在 4/6）
./scripts/check.sh; echo "EXIT=$?"

# 3. 確認唯一失敗規則與數量
uv run --frozen ruff check src/hotstock tests --statistics

# 4. 確認其餘步驟本身是通過的
uv lock --check
bash -n scripts/check.sh
uv run --frozen ruff format --check src/hotstock tests   # exit 0
uv run --frozen mypy src/hotstock                        # exit 0（含一則 note）
uv run --frozen pytest                                   # 4 passed

# 5. check.sh 基本性質
test -x scripts/check.sh && echo executable
cd /tmp && ~/projects/hotstock-tw/scripts/check.sh; echo "EXIT=$?"   # 非 root 亦可定位
cd ~/projects/hotstock-tw

# 6. 範圍核對
git check-ignore -v .ruff_cache .mypy_cache .pytest_cache
git diff --exit-code HEAD -- uv.lock && echo "lock 未變"
git diff --exit-code HEAD -- src/db.py src/sources.py src/pipeline.py run_news.py requirements.txt config/sources.yaml && echo "舊檔零變更"
ls src/hotstock/domain src/hotstock/data src/hotstock/cli.py 2>&1   # 預期 No such file
find tests -maxdepth 2 -type f -print | sort

# 7. 兩項文件修正
rg -n "proposed|專案經理明確核准" docs/adr/README.md
rg -n "完全一致" README.md      # 預期無符合項

# 8. Git 數字
git diff --numstat
git diff --check
```

**預期：** 步驟 2 exit 1；步驟 3 顯示 10 × RUF002；步驟 4 除 lint 外皆 exit 0；步驟 6 全部符合；步驟 7 第二條無符合項。

---

## 八、停止聲明

**B0-R02 未完成，狀態 BLOCKED，工作已停止。**

- 阻塞於 §六 Q-1，**需專案經理裁示後才能繼續**。
- **未自行採用任何預設方案**——未加 `ignore`、未加 `noqa`、未從 `select` 移除 `RUF`、未改寫任何 docstring。
- §7.8 的 fail-fast 反向驗證**未執行**，暫存失敗檔**從未建立**。
- **B0-R03 尚未開始。** 未建立 enum、error、PIT model 或任何 domain／data／adapters／CLI 內容。
- 未 commit 本輪變更；未 push、merge、rebase、amend、squash。
- 等待 `docs/reviews/member-b/` 中新的檢查報告。

<!-- REPORT-COMPLETE -->
