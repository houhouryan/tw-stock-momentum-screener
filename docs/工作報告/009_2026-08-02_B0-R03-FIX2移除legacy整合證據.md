# 工作報告 009｜B0-R03-FIX2 移除 legacy 整合證據

| 項目 | 內容 |
|---|---|
| 日期 | 2026-08-02 |
| 輪次 | 009（**B0-R03-FIX2**） |
| 狀態 | **COMPLETE** |
| 耗時 | 約 40 分鐘 |
| Branch | `feature/b0-skeleton` |
| Base commit | `72d3cc059b41e7f59e42e57c5cb123a12fc3646e` |
| **Head commit** | **uncommitted** |
| 對應檢查報告 | `docs/reviews/member-b/20260802-201412_B0-R03-FIX1複核與FIX2整合收尾指南_review.md` §7～§11 |
| 性質 | **純文件輪。唯一可新增檔案為本檔，不得修改任何既有檔案。** |

---

## 〇、本輪規劃步驟

> 本節於執行前寫定。執行中若發現規劃有誤，**不修改本節**，一律記入「六、問題、偏差與風險」。

### 0.1 唯一目標

補上稽核鏈中缺少的最後一段：把「報告 008 凍結後」發生的專案經理決策、legacy 刪除與刪除後全綠證據，記錄成可獨立重現的工作報告。

**本輪不修改任何程式、設定或既有文件。**

### 0.2 我必須避免的四種錯誤陳述（檢查報告 §7.3）

| # | 禁止 | 本檔的正確寫法 |
|---:|---|---|
| 1 | 把審查者執行的刪除寫成我先前已完成 | §三 明確標示執行者為審查者，依專案經理授權 |
| 2 | 把我在報告 008 建議的方案 A 寫成已核准 | §二 明寫**方案 A 未被採用**，實際採用完整 legacy 刪除 |
| 3 | 因 line 294 whitespace 回寫 frozen 報告 | §五 5.9 單獨揭露為既知例外，不修改該檔 |
| 4 | 泛稱「全部檔案完全無 whitespace」 | §五 5.9 明確區分「本輪新檔無命中」與「frozen 檔有一個既知命中」 |

### 0.3 步驟表

| # | 動作 | 完成判準 | 驗證方式 | 依賴 | 預估 |
|---:|---|---|---|---|---:|
| 1 | 確認檢查報告最後一行是唯一的 marker | 最後非空白行等於 marker，且全檔僅一處為結尾 | `grep -v '^[[:space:]]*$' \| tail -1` | — | 2m |
| 2 | 記錄 branch、base、Head、staged diff 為空 | staged 為空 | `git diff --cached --quiet` | 1 | 2m |
| 3 | 驗證報告 008 與兩份 FIX1 檢查報告雜湊，並記錄本檢查報告雜湊 | 三者符合 §2.2 | `sha256sum` | 2 | 3m |
| 4 | 建立本檔，寫定規劃；表頭自始寫 `uncommitted` | 表頭無「待補」 | 目視 | 3 | 12m |
| 5 | 按時間順序記錄五個階段 | 五階段齊備且執行者標示正確 | 目視對照 §6 圖 | 4 | 10m |
| 6 | 逐字列出七個 legacy path 並驗證皆不存在 | 七者皆不存在 | `test ! -e` | 2 | 3m |
| 7 | 驗證 deletion set 恰為七檔 | 無第八個 | `git diff --name-only --diff-filter=D` | 6 | 2m |
| 8 | 驗證 modification set 恰為五檔 + untracked ADR-0002 | 五檔相符 | `--diff-filter=M` | 7 | 2m |
| 9 | 閱讀 ADR-0002，確認五要素；確認 ADR-0001 無 diff | 核准人／範圍／主分支影響／資料不刪除／Git recovery 齊備 | 閱讀 + `git diff --exit-code` | 8 | 5m |
| 10 | 檢查 `pyproject.toml`／`scripts/check.sh` diff 只有註解 | 無功能變更 | `git diff` 逐行 | 8 | 5m |
| 11 | `uv lock --check` 與 lockfile diff | 皆通過 | 命令 | 2 | 2m |
| 12 | 驗證 §3.2 八個 path 雜湊 | 全部一致 | `sha256sum` | 3 | 3m |
| 13 | 驗證 `py.typed` 為 regular、0 bytes、644、可定位 | `644 0` 與 `True b''` | `stat` + importlib | 12 | 2m |
| 14 | 重做 context mutation 負向探針 | 探針 exit 0 | §9 指定命令 | 13 | 3m |
| 15 | error targeted tests | 50／50、exit 0 | pytest | 14 | 2m |
| 16 | 全部 domain tests | 128／128、exit 0 | pytest | 15 | 2m |
| 17 | 正常無環境變數無快取 mypy | exit 0、4 source files、`hotstock.domain` 不在 unused | mypy | 16 | 3m |
| 18 | 完整 gate | 六段皆執行、132／132、exit 0 | `./scripts/check.sh` | 17 | 3m |
| 19 | `/tmp` 離線 wheel | 恰一個 0-byte marker、repo 無 `dist/` | uv build + unzip | 18 | 4m |
| 20 | legacy import 與 R04 path 搜尋 | 無命中；三個 R04 path 不存在 | rg + test | 18 | 3m |
| 21 | whitespace：本輪新檔無命中；frozen 193902 只有 line 294 | 兩段分別驗證並分別陳述 | `git diff --check` + 兩組 rg | 20 | 4m |
| 22 | 完整 status，核對 27 = 12 tracked + 15 untracked | 相符；不符即停止 | `git status --short -uall` | 21 | 3m |
| 23 | 確認 staged 為空、未 commit、未 push、R04 未開始 | 四者確認 | git | 22 | 2m |
| 24 | 補完報告八節 | §10 十項必填齊備 | 目視 | 5–23 | 20m |
| 25 | 機器計算 checklist total／checked／unchecked | 相加一致 | `rg -c` | 24 | 3m |
| 26 | **全文 residual search**：測試數、path 數、狀態、base/head、方案與執行者 | 每處皆對應 raw output | `rg -n` | 25 | 8m |
| 27 | **最後**加入唯一 `REPORT-COMPLETE`，其後單一 LF，加入後不再修改 | `od` 顯示 marker 後單一 `0a` | `tail -c 32 \| od` | 26 | 3m |

**預估合計：約 1 小時 51 分。** 依前七輪校準（實際／估計比 0.29–0.50），實際預期約 35–55 分鐘。

### 0.4 停止條件（檢查報告 §11 的十項）

任一觸發即停止，不改任何既有檔，只在本檔完整寫出前因後果：

檢查報告未完成、凍結雜湊改變、七個 legacy path 有殘留或出現第八個 deletion、文件 diff 超出 §4.3、`pyproject.toml`／`check.sh` 有功能改動、`uv.lock` 有 diff、任一驗證失敗、status 不是 12+15、出現 line 294 以外的新 whitespace、發現未揭露的同時修改或 R04／commit／push、需要修改本檔以外的檔案。

### 0.5 風險與退路

| 編號 | 風險 | 步驟 | 退路 |
|---|---|---|---|
| R-1 | status 不是預期的 27 個 entry | 22 | §11 明訂**立即停止，不得自行修正未知差異**。據實記錄實際數字與差異項 |
| R-2 | 複製審查者數字而未自行重跑 | 14–19 | §10 明文禁止。每一項都由我實際執行並貼出自己的輸出 |
| R-3 | 誤把方案 A 寫成已核准 | 5 | §0.2 第 2 項；§二 專節說明方案 A **未被採用** |
| R-4 | 誤把審查者的刪除寫成自己的工作 | 5 | §0.2 第 1 項；§三 逐項標示執行者 |
| R-5 | whitespace 陳述過度泛化 | 21 | §0.2 第 4 項；分成兩段分別陳述，不合併成一句 |

### 0.6 本輪明確不做

- **不修改任何既有檔案**（程式、測試、設定、README、ADR、既有工作報告、既有檢查報告）
- 不恢復任何 legacy path
- 不新增 ignore、環境變數、替代 mypy 命令或任何 workaround
- 不 commit、不 stage、不 push、不 merge、不 rebase、不 amend、不 squash
- 不修改或刪除既有報告的完成標記
- **不開始 B0-R04**

---

## 一、Checklist

> 原樣複製自檢查報告 §8 的 26 項。

- [x] 1. 確認本檢查報告最後一行是唯一的 `REVIEW-COMPLETE`，再開始工作。
- [x] 2. 記錄 branch `feature/b0-skeleton`、base `72d3cc059b41e7f59e42e57c5cb123a12fc3646e`、Head `uncommitted` 與 staged diff 為空。
- [x] 3. 驗證工作報告 008 SHA-256 仍為 `38e3510fc37ebd82992dd8e5c89659663c9b816c85659df5177c642491f23186`，marker 恰一個且為最後一行。
- [x] 4. 驗證兩份既有 FIX1 檢查報告雜湊仍為 §2.2 所列值，並記錄本檢查報告實際 SHA-256。
- [x] 5. 新增工作報告 009，先寫規劃、風險與停止條件；表頭從一開始寫 `Head commit | uncommitted`。
- [x] 6. 按時間順序記錄：報告 008 正確停止 → 專案經理詢問分支影響 → 核准完整刪除 → 審查者執行刪除與文件同步 → 刪除後驗證全綠。
- [x] 7. 逐字列出 §4.2 七個 legacy path，驗證七檔都不存在。
- [x] 8. 驗證 `git diff --name-only --diff-filter=D` 恰為七檔，沒有第八個 tracked deletion。
- [x] 9. 驗證 tracked modification 恰為 `README.md`、`docs/adr/README.md`、`pyproject.toml`、`scripts/check.sh`、`tests/README.md`，另有 untracked ADR-0002。
- [x] 10. 閱讀 ADR-0002，確認它記錄核准人、刪除範圍、主分支影響、資料不刪除與 Git recovery；確認 ADR-0001 本體沒有 diff。
- [x] 11. 檢查 `pyproject.toml` 與 `scripts/check.sh` diff，確認只有註解改寫，mypy 與 gate 命令沒有功能變更。
- [x] 12. 執行 `uv lock --check` 並確認 `git diff --exit-code HEAD -- uv.lock` 無 diff。
- [x] 13. 驗證 §3.2 所列八個 path（含 `py.typed`）的雜湊全部一致。
- [x] 14. 驗證 `src/hotstock/py.typed` 為 regular file、0 bytes、mode 644，editable resource 可定位。
- [x] 15. 重做 context mutation 負向探針，確認 property 與 `to_dict()` 的回傳值都不能污染後續輸出或注入 secret。
- [x] 16. 執行 `tests/unit/domain/test_errors.py`，記錄 collected／passed = 50／50 與 exit 0。
- [x] 17. 執行全部 domain tests，記錄 collected／passed = 128／128 與 exit 0。
- [x] 18. 執行未加環境變數的 `mypy --no-incremental src/hotstock/domain`，確認 exit 0、4 source files，且 `hotstock.domain` 不在 unused override 清單。
- [x] 19. 執行完整 `./scripts/check.sh`，確認六段全部執行、132／132 tests、整體 exit 0。
- [x] 20. 在 `/tmp` 離線建立 wheel，確認恰含一個 0-byte `hotstock/py.typed`，且 repo 沒有新增 `dist/`。
- [x] 21. 搜尋 `src/hotstock` 與 `tests`，確認沒有 legacy import；確認 `src/hotstock/data`、`src/hotstock/adapters`、`src/hotstock/cli.py` 等 R04 path 不存在。
- [x] 22. 執行 tracked `git diff --check`；另檢查本輪新檔尾端空白。單獨揭露 frozen 193902 報告只在 line 294 有既知命中，不得修改它。
- [x] 23. 記錄完整 `git status --short --untracked-files=all`：建立報告 009 後預期 27 個 path entry＝12 tracked changes＋15 untracked files；若不同立即停止，不得自行修正未知差異。
- [x] 24. 確認 staged diff 仍為空、R03 未 commit、未 push，R04 未開始。
- [x] 25. 機器計算本工作表 total／checked／unchecked，核對 `checked + unchecked = total`。
- [x] 26. 全文 residual search，核對所有測試數、path 數、狀態、base/head、方案與執行者；最後才加入唯一的 `REPORT-COMPLETE`，加入後不得再改，停止等待檢查。

**計數：26 項，26 項已勾選，0 項未勾選**（26 + 0 = 26，相加一致；計數命令見 §五 5.11）。

---

## 二、專案經理決策：方案 A 未被採用

> 本節專門釐清一件容易被誤讀的事：**我在工作報告 008 §七 Q-1 建議的方案 A 並沒有被採用。**

### 2.1 我建議的與實際採用的

| 項目 | 內容 |
|---|---|
| **我在報告 008 建議的方案 A** | 修改 `scripts/check.sh` 第 37 行，`mypy src/hotstock` → `mypy -p hotstock`（單行改動） |
| **專案經理實際核准的** | **從目前 feature branch 移除整套 legacy 新聞爬蟲**（七個 tracked path） |
| **兩者關係** | 專案經理**沒有採用**我的方案 A。`scripts/check.sh` 的 mypy 命令**至今未改**（§四 4.4 附 diff 佐證） |

我在報告 008 也列了方案 C「刪除 legacy `src/__init__.py`」，但當時評估其風險為「`run_news.py` 有 `from src.pipeline import run`，刪除可能影響舊工具」，且受 DEC-003 保護。專案經理的決策範圍**比方案 C 更大**——不是只刪一個 marker 檔，而是移除整套舊工具，因此「影響舊工具」不再是顧慮。

### 2.2 為什麼這個區別重要

若把方案 A 寫成已核准，未來成員會誤以為 `scripts/check.sh` 曾被改過，進而懷疑品質 gate 的歷史一致性。實際上 gate 命令從 R02 建立至今**功能完全未變**。

---

## 三、時間序與責任歸屬

> 依檢查報告 §8 第 6 項，按時間順序記錄五個階段，並明確標示每一步的執行者。

| # | 時點 | 事件 | **執行者** |
|---:|---|---|---|
| 1 | 報告 008 收尾 | FIX1 完成 `py.typed`、context 封裝、測試依據更正；正常 mypy 因 legacy `src/__init__.py` 造成同檔雙模組名稱而失敗，報告如實標 `BLOCKED`，28 項中 2 項保留未勾選，未假稱 gate 通過 | **組員 B（我）** |
| 2 | 報告 008 凍結後 | 詢問「feature branch 刪除檔案是否立即影響主分支」，確認須 merge／squash merge／明確挑入才會影響 | **專案經理** |
| 3 | 同上 | 明確指示「刪除新聞爬蟲的舊檔，可以繼續」——正式滿足 ADR-0001 DEC-003 所要求的授權 | **專案經理** |
| 4 | 2026-08-02 20:14 前 | 依授權移除七個 tracked legacy path、建立 ADR-0002、同步五份現行說明文件 | **審查者** |
| 5 | 同上 | 刪除後獨立重跑正常 mypy 與完整 gate，六段全執行、132 passed | **審查者** |
| 6 | 本輪 | 獨立重現階段 4–5 的全部證據，並補上此稽核鏈 | **組員 B（我）** |

### 3.1 明確聲明

- **七個 legacy path 的刪除、ADR-0002 的建立與五份文件的同步，都是審查者依專案經理授權執行的，不是我做的。**
- 我在本輪**只新增本檔一個檔案**，未修改任何既有檔案（§四 4.5 附 Git 佐證）。
- 我對階段 4–5 的證據**全部自行重跑**，未複製審查者的數字（§五 各節皆為我本輪的實際輸出）。

### 3.2 為什麼工作報告 008 不得回寫

報告 008 描述的是**它完成當下**的工作樹：當時 `src/__init__.py` 仍存在，mypy 確實失敗，`BLOCKED` 是正確且誠實的記載。

若回頭改寫成「已通過」，就會抹掉「工程師遇到授權外的修法時正確停止」這段稽核事實，也會讓 ADR-0002 的決策失去觸發脈絡。依 ADR-0001 DEC-012，既有工作報告不可覆寫、刪除或改名。**報告 008 雜湊經驗證未變**（§五 5.1）。

---

## 四、範圍驗證

### 4.1 七個 legacy path（§4.2 逐字）

```console
✅ 已不存在 run_news.py
✅ 已不存在 src/__init__.py
✅ 已不存在 src/db.py
✅ 已不存在 src/sources.py
✅ 已不存在 src/pipeline.py
✅ 已不存在 config/sources.yaml
✅ 已不存在 requirements.txt
```

### 4.2 deletion set 恰為七檔，無第八個

```console
$ git diff --name-only --diff-filter=D | sort
config/sources.yaml
requirements.txt
run_news.py
src/__init__.py
src/db.py
src/pipeline.py
src/sources.py

$ git diff --name-only --diff-filter=D | wc -l
7
```

### 4.3 modification set 恰為五檔

```console
$ git diff --name-only --diff-filter=M | sort
README.md
docs/adr/README.md
pyproject.toml
scripts/check.sh
tests/README.md

$ git diff --name-only --diff-filter=M | wc -l
5
```

另有 untracked 的 `docs/adr/ADR-0002-移除legacy新聞爬蟲.md`（見 §五 5.12 的 status）。

### 4.4 `pyproject.toml` 與 `scripts/check.sh` 只有註解改寫

```diff
- # wheel 只打包新系統；舊 src/*.py 明確排除在外（ADR-0001 DEC-002）
+ # wheel 只打包新系統；legacy 新聞爬蟲已依 ADR-0002 移除
- # 舊 src/*.py、run_news.py 依 ADR-0001 DEC-002 不屬新主線，
- # 不掃描、不格式化、不修正、不測試，也不為其加任何 ignore。
+ # legacy 新聞爬蟲已依 ADR-0002 移除；歷史稽核文件不納入程式掃描。
- # 掃描範圍只含新系統（src/hotstock、tests）。舊 src/*.py、run_news.py
- # 依 ADR-0001 DEC-002 不屬新主線，不在此檢查。
+ # 掃描範圍只含新系統（src/hotstock、tests）。
+ # legacy 新聞爬蟲已依 ADR-0002 移除。
```

**全部 diff 行都以 `#` 開頭。** `[tool.mypy]`、`[tool.ruff]`、`[tool.pytest.ini_options]` 的設定值與 `check.sh` 的六段命令**沒有任何功能變更**——特別是 `mypy src/hotstock` 未改成 `mypy -p hotstock`（呼應 §二 2.1）。

### 4.5 ADR 與 lockfile

```console
$ git diff --exit-code HEAD -- docs/adr/ADR-0001-B0基線決策.md
ADR-0001 無 diff ✅
$ git diff --exit-code HEAD -- uv.lock
uv.lock 無 diff ✅
$ uv lock --check
exit 0 ✅
```

**ADR-0002 內容核對**（§8 第 10 項五要素）：

| 要素 | ADR-0002 是否記載 |
|---|:---:|
| 核准人 | ✅ 專案經理 |
| 刪除範圍 | ✅ 七個 tracked path 逐一列出 |
| 主分支影響 | ✅ 須 merge／squash merge／明確挑入才生效 |
| 資料不刪除 | ✅ 未追蹤的資料庫、擷取資料與日誌均未刪除 |
| Git recovery | ✅ 原始內容保留於 commit `272e13f` |
| 與 DEC-003 關係 | ✅ 明確取代 DEC-003 的保留要求，並更新 DEC-007 的 migration note |

---

## 五、技術驗證證據

> 以下**全部為我本輪自行重跑**的輸出，非複製審查者數字。

### 5.1 凍結雜湊（§8 第 3–4 項）

```console
$ sha256sum <四份報告>
38e3510fc37ebd82992dd8e5c89659663c9b816c85659df5177c642491f23186  工作報告 008
39bcb65fde36ff5b210b24b3a4aa19447426740b9d2c688a52ad390197c55ac8  20260802-193902 review
71da9c494ac6e26ce7c072ae68b4e35871b73cf624cd1cf6581f947a60902321  20260802-194438 review
19e712568db699a08ca86ca4669ac2c65be539c06baf9fc5724613e21dc587ff  20260802-201412 review（本輪指南，實測值）
```

前三者與檢查報告 §2.2 預期**逐字相符**；第四者為本檢查報告的實測 SHA-256（§8 第 4 項要求記錄）。

報告 008 的 marker 檢查：最後非空白行為 `<!-- REPORT-COMPLETE -->`，全檔僅此一處作結尾。

### 5.2 §3.2 八個 path 雜湊（§8 第 13 項）

| 檔案 | SHA-256 | 相符 |
|---|---|:---:|
| `src/hotstock/domain/errors.py` | `6fa315e48a32ec126827d0db239e7c444ed111f33a1bd60571ef980e70b432c2` | ✅ |
| `tests/unit/domain/test_errors.py` | `2200cc7c152644ef136e3c22da030f77749c6725cfdfddb33b645fae50c3b3d2` | ✅ |
| `tests/unit/domain/test_pit_metadata.py` | `b13cbf743b854b4742c519f79a166f410a39047bbf74f0db02d21cbf02c036e3` | ✅ |
| `src/hotstock/py.typed` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | ✅ |
| `src/hotstock/domain/__init__.py` | `7b756923a23e384d9162035cb633e1f7b29ff85e399272ebc72df4419e512853` | ✅ |
| `src/hotstock/domain/enums.py` | `537910853aeb3478bdf3eb7f251a7334921f31ed0766049ef4a4a47b4b5b1c32` | ✅ |
| `src/hotstock/domain/models.py` | `3929475ebbf680af05190a470ec03a13ea3a67652cc614778d66e1f3b82af105` | ✅ |
| `tests/unit/domain/test_enums.py` | `a71956efdf162af055806a9fa27a209a9c0e8c11b8c619eda33dad4b9239a5a8` | ✅ |

**八個全部一致，FIX1 的程式與測試在 legacy 刪除後未被改動。**

### 5.3 `py.typed`（§8 第 14 項）

```console
$ stat -c '%a %s %n' src/hotstock/py.typed
644 0 src/hotstock/py.typed

$ uv run --frozen python -c "from importlib.resources import files; p=files('hotstock').joinpath('py.typed'); print(p.is_file(), p.read_bytes())"
True b''
```

### 5.4 context mutation 負向探針（§8 第 15 項）

```console
$ uv run --frozen python -c "...（檢查報告 §9 指定的探針）..."
mutation probe: PASS
probe exit=0
```

探針同時驗證：注入 `api_token=SECRET` 無效、`context` 回傳值的巢狀 list mutation 無效、`to_dict()` 回傳值的巢狀 mutation 無效，且 `'SECRET' not in str(e.to_dict())`。

### 5.5 測試（§8 第 16–17 項）

```console
$ uv run --frozen pytest tests/unit/domain/test_errors.py -q
50 passed        exit=0

$ uv run --frozen pytest tests/unit/domain -q
128 passed       exit=0
```

collected／passed = **50／50** 與 **128／128**。

### 5.6 正常 mypy（§8 第 18 項）

**未加任何環境變數、未加 `--explicit-package-bases`、未改成 `mypy -p`：**

```console
$ uv run --frozen mypy --no-incremental src/hotstock/domain
pyproject.toml: note: unused section(s): module = ['hotstock.data', 'hotstock.data.*']
Success: no issues found in 4 source files
exit=0
```

**`hotstock.domain` 不在 unused override 清單中**，代表 strict override 確實生效。unused 只剩尚未建立的 `hotstock.data`，屬預期。

> 這正是報告 008 §六 B-1 診斷 D 所預測的結果：移除 legacy `src/__init__.py` 後，同檔雙模組名稱衝突消失。**當時的診斷成立。**

### 5.7 完整 gate（§8 第 19 項）

```console
$ ./scripts/check.sh
▶ 1/6 lockfile 漂移檢查    PASS
▶ 2/6 shell 語法檢查       PASS
▶ 3/6 format 檢查          PASS
▶ 4/6 lint                 PASS
▶ 5/6 型別檢查             PASS
▶ 6/6 測試                 132 passed
GATE_EXIT=0
```

**六段全部執行**（不同於報告 008 當時只執行五段）。測試加總 53 enum + 50 error + 25 PIT + 4 smoke = **132**。

### 5.8 離線 wheel（§8 第 20 項）

```console
$ uv build --offline --wheel --out-dir /tmp/hotstock-r03-fix2.XXXXXX
Successfully built hotstock_tw-0.1.0-py3-none-any.whl

$ unzip -Z1 <wheel> | sort
hotstock/__init__.py
hotstock/domain/__init__.py
hotstock/domain/enums.py
hotstock/domain/errors.py
hotstock/domain/models.py
hotstock/py.typed
hotstock_tw-0.1.0.dist-info/METADATA
hotstock_tw-0.1.0.dist-info/RECORD
hotstock_tw-0.1.0.dist-info/WHEEL

$ unzip -Z1 <wheel> | grep -c '^hotstock/py\.typed$'
1
$ unzip -p <wheel> hotstock/py.typed | wc -c
0
$ ls -d dist
ls: 無法存取 'dist': 沒有此一檔案或目錄
```

**恰含一個 `hotstock/py.typed`，0 bytes；wheel 內無任何 legacy 模組；repo 未產生 `dist/`。**

### 5.9 whitespace（§8 第 22 項）——**分兩段陳述**

**(a) tracked diff：**

```console
$ git diff --check
（無輸出）  exit=0
```

**(b) 本輪新檔與現行檔（ADR-0002、194438、201412、報告 007／008／009、domain 程式與測試）：**

```console
$ rg -n '[[:blank:]]+$' <上述檔案>
（無輸出）  exit=1 → 無命中
```

**(c) frozen `20260802-193902` 報告——單一既知例外：**

```console
$ rg -n '[[:blank:]]+$' docs/reviews/member-b/20260802-193902_B0-R03決策核准與FIX1指南_review.md
294:
```

依檢查報告 §2.3，**這是審查者造成、且已於 `20260802-194438` 更正報告 §4 正式揭露的既知問題**。該檔已寫入完成標記並凍結，**我未修改它**，也未為此建立 ignore 或調整工具設定。

> 我**不宣稱**「全部檔案完全無 whitespace」。正確陳述是：**本輪新檔與現行程式無任何命中；frozen 193902 報告有一個既知且已揭露的命中（line 294）。**

### 5.10 legacy import 與 R04 path（§8 第 21 項）

```console
$ rg -n 'from src|import src|src\.db|src\.sources|src\.pipeline|run_news' src/hotstock tests
（無輸出）  exit=1 → 無命中

✅ src/hotstock/data 不存在
✅ src/hotstock/adapters 不存在
✅ src/hotstock/cli.py 不存在
```

### 5.11 本報告 checklist 機器計數（§8 第 25 項）

```console
$ sed -n '/^## 一、Checklist/,/^\*\*計數/p' 009_...md | rg -c '^- \[x\]'
26
$ sed -n '/^## 一、Checklist/,/^\*\*計數/p' 009_...md | rg -c '^- \[[x ]\]'
26
```

total = 26、checked = 26、unchecked = 0，**26 + 0 = 26** 相加一致。

### 5.12 完整 Git status（§8 第 23–24 項）

```text
 M README.md
 D config/sources.yaml
 M docs/adr/README.md
 M pyproject.toml
 D requirements.txt
 D run_news.py
 M scripts/check.sh
 D src/__init__.py
 D src/db.py
 D src/pipeline.py
 D src/sources.py
 M tests/README.md
?? docs/adr/ADR-0002-移除legacy新聞爬蟲.md
?? docs/reviews/member-b/20260802-193902_..._review.md
?? docs/reviews/member-b/20260802-194438_..._review.md
?? docs/reviews/member-b/20260802-201412_..._review.md
?? docs/工作報告/007_...md
?? docs/工作報告/008_...md
?? docs/工作報告/009_...md          ← 本檔，本輪唯一新增
?? src/hotstock/domain/__init__.py
?? src/hotstock/domain/enums.py
?? src/hotstock/domain/errors.py
?? src/hotstock/domain/models.py
?? src/hotstock/py.typed
?? tests/unit/domain/test_enums.py
?? tests/unit/domain/test_errors.py
?? tests/unit/domain/test_pit_metadata.py
```

```console
總 entry: 27   tracked changes: 12   untracked: 15
預期: 27 = 12 + 15
✅ 完全相符
```

**12 tracked changes = 5 modifications + 7 deletions**（§四 4.2、4.3）。**15 untracked** = ADR-0002 × 1 + 檢查報告 × 3 + 工作報告 × 3 + domain 程式 × 4 + `py.typed` × 1 + 測試 × 3。

```console
$ git diff --cached --quiet
staged diff: 空 ✅

$ git rev-parse HEAD
72d3cc059b41e7f59e42e57c5cb123a12fc3646e     ← 仍為 R02 closure commit，R03 未 commit
```

未 push（本地領先 origin 3 個 commit，皆未推送）。

---

## 六、問題、偏差與風險

### V-1　報告 008 的診斷 D 已被實測證實

報告 008 §六 B-1 曾以「暫時移開後立即還原」的方式做診斷 D，結論是移除 legacy `src/__init__.py` 可讓 mypy exit 0 且 strict override 生效。

專案經理採用的完整刪除方案涵蓋該檔，**本輪 §五 5.6 的實測結果與當時診斷完全一致**。當時的診斷方法與結論成立。

### V-2　我建議的方案 A 未被採用（非偏差，屬記錄）

見 §二。專案經理選擇了範圍更大但更根本的解法。`scripts/check.sh` 的 mypy 命令至今未改，§四 4.4 附 diff 佐證。

### V-3　本輪未修改任何既有檔案

`git diff --name-only --diff-filter=M` 的五個檔案與七個刪除，**都不是我在本輪造成的**——它們在我開工前就已存在於工作樹（由審查者依授權執行）。我本輪唯一的檔案動作是新增本檔。

### V-4　工時：估 1h51m，實際約 40 分鐘

| 輪次 | 估計 | 實際 | 比值 |
|---|---:|---:|---:|
| R00 | 3h | 1.5h | 0.50 |
| R01 | 3h25m | 1h | 0.29 |
| R02 | 3h35m | 1h10m | 0.33 |
| R02-FIX1 | 88m | 35m | 0.40 |
| R02-FIX2 | 35m | 15m | 0.43 |
| R03 | 3h48m | 75m | 0.33 |
| R03-FIX1 | 2h20m | 55m | 0.39 |
| **R03-FIX2** | **1h51m** | **約 40m** | **0.36** |

八輪的比值穩定落在 0.29–0.50，中位數約 0.37。

### V-5　我的等待偵測邏輯曾誤判（已修正，非本專案檔案問題）

本輪開工前，我的背景輪詢曾因使用 `grep -q "REVIEW-COMPLETE"` **搜尋全檔**而誤判——檢查報告 `20260802-201412` 第 297 行的指示文字含該字串，但當時檔尾尚無 marker。

我在讀取前先檢查「最後一個非空白行」，發現該報告當時明寫「完成訊號會等到獨立全文自驗…才以單獨最後步驟加入」，因此**未讀取內容、未開工**，並把偵測改為比對最後一個非空白行後重新等待。marker 出現後才正式開工。

此為我方工具問題，不涉及專案檔案，據實記錄。

### 範圍外變更

**無。** 本輪唯一新增檔案為本檔；未修改、刪除或改名任何既有檔案。

---

## 七、待決事項

**無。**

所有驗證項目均通過，未觸發檢查報告 §11 的任一停止條件；`git status` 為預期的 27 = 12 + 15。

---

## 八、停止聲明

**B0-R03-FIX2 已完成，工作已停止。**

- 本輪**只新增工作報告 009**，未修改、刪除或改名任何既有檔案。
- 七個 legacy path 維持 deleted，未恢復任何一個；未新增 ignore、環境變數、替代 mypy 命令或任何 workaround。
- §3.2 八個 path 與三份凍結報告雜湊全部未變。
- 正常 mypy exit 0、完整 gate 六段全執行 **132 passed** exit 0、mutation probe PASS、wheel 含 0-byte marker、`uv.lock` 無 diff——**全部由我本輪自行重跑**。
- 已明確區分：**我的 FIX1 工作**、**專案經理的決策**、**審查者執行的 legacy 刪除與文件同步**。
- 已明寫報告 008 建議的**方案 A 未被採用**。
- frozen `193902` 報告 line 294 的既知 whitespace 已單獨揭露，**未回寫該檔**。
- **未 commit、未 stage、未 push**、未 merge、未 rebase、未 amend、未 squash。
- **B0-R04 尚未開始。**
- 等待 `docs/reviews/member-b/` 中新的檢查報告。

<!-- REPORT-COMPLETE -->
