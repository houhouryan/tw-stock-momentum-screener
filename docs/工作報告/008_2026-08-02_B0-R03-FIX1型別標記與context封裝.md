# 工作報告 008｜B0-R03-FIX1 型別標記與 context 封裝

| 項目 | 內容 |
|---|---|
| 日期 | 2026-08-02 |
| 輪次 | 008（**B0-R03-FIX1**） |
| 狀態 | **BLOCKED** |
| 阻塞原因 | 新增已核准的 `py.typed` 後，正常 mypy 出現**新錯誤**：`Source file found twice under different module names`。修法需改 `pyproject.toml` 或 `scripts/check.sh`（§6 不得修改），或刪除 legacy `src/__init__.py`（受 DEC-003 保護）。觸發 §13 停止條件第 1、3 項。詳見 §七 Q-1 |
| 耗時 | 約 55 分鐘 |
| Branch | `feature/b0-skeleton` |
| **Base commit** | `72d3cc059b41e7f59e42e57c5cb123a12fc3646e` |
| **Head commit** | **uncommitted** |
| 對應檢查報告 | `docs/reviews/member-b/20260802-193902_B0-R03決策核准與FIX1指南_review.md` §5～§11 |

> 表頭的 Head commit 自建立時即填 `uncommitted`，**不使用「待補」**——這是檢查報告 §8.3 對 R03-F04 的明確要求。

---

## 〇、本輪規劃步驟

> 本節於執行前寫定。執行中若發現規劃有誤，**不修改本節**，一律記入「六、問題、偏差與風險」。

### 0.1 唯一目標

四件事：新增已核准的 `py.typed`、修正 error context 的建構後繞過漏洞、把測試設計依據收回 SDD、正式更正報告 007 的表頭錯誤。

### 0.2 步驟表

| # | 動作 | 產出檔案 | 完成判準 | 驗證方式 | 依賴 | 預估 |
|---:|---|---|---|---|---|---:|
| 1 | 記錄 branch、base、Head、起始 status | 本檔表頭 | base = `72d3cc0`，Head 寫 `uncommitted` | `git rev-parse`／`git status --short` | — | 2m |
| 2 | 記錄八個基線 SHA-256 並確認與 §3 一致 | 本檔 §四 | 八個逐字相符 | `sha256sum` | 1 | 3m |
| 3 | 寫定本規劃並原樣複製 §10 的 27 項清單 | 本檔 §〇、§一 | 27 項逐字複製 | 目視對照 §10 | 2 | 10m |
| 4 | 新增 0-byte、mode 644 的 `py.typed` | `src/hotstock/py.typed`（新增） | `stat` 顯示 `644 0` | §11.1 | 3 | 2m |
| 5 | 驗證 `py.typed` 可由 editable package 定位 | 命令輸出 | `importlib.resources` 回傳 `True b''` | §11.1 | 4 | 2m |
| 6 | 改 `errors.py`：context 改為私有 JSON snapshot | `src/hotstock/domain/errors.py`（修改） | 無公開可變 dict 屬性 | 步驟 8 | 3 | 15m |
| 7 | 建立每次回傳 fresh deep copy 的 `context` property 與 `to_dict()` | 同上 | 三種外部 mutation 皆無法污染 | 步驟 8 | 6 | 10m |
| 8 | 在 `test_errors.py` 加入 §6.3 的五項 mutation 負向測試 | `tests/unit/domain/test_errors.py`（修改） | 五項齊備，且既有 44 tests 不退步 | 步驟 9 | 7 | 15m |
| 9 | 執行 error targeted test | 命令輸出 | 保存 collected／passed 與 exit code | `pytest .../test_errors.py` | 8 | 3m |
| 10 | 只改 `test_pit_metadata.py` 的 module docstring，改引 SDD | `tests/unit/domain/test_pit_metadata.py`（修改） | 移除舊新聞工具、`fetched_at`、`max(published_at` 敘述；**25 個測試行為不變** | 步驟 11、12 | 3 | 5m |
| 11 | legacy reference 搜尋 | 命令輸出 | §7.2 的 `rg` **無輸出** | 同左 | 10 | 2m |
| 12 | 只格式化／lint 本輪變更檔 | — | format 與 lint 皆 exit 0 | ruff | 10 | 3m |
| 13 | **正常、無環境變數、無快取** mypy | 命令輸出 | exit 0 | `mypy --no-incremental src/hotstock/domain` | 12 | 3m |
| 14 | 核對 mypy unused override | 命令輸出 | **可有 `hotstock.data`，不得有 `hotstock.domain`** | 同上輸出 | 13 | 2m |
| 15 | 全部 domain tests | 命令輸出 | 保存實際測試數與 exit code | `pytest tests/unit/domain` | 13 | 2m |
| 16 | 完整 `./scripts/check.sh` | 命令輸出 | **六段皆執行**且整體 exit 0 | 同左 | 15 | 3m |
| 17 | 離線 wheel build 並確認含 0-byte marker | 命令輸出 | wheel 內恰一個 `hotstock/py.typed`，`wc -c` 為 0 | §11.3 | 16 | 5m |
| 18 | `uv lock --check` 與 lockfile diff | 命令輸出 | 兩者皆通過 | 同左 | 16 | 2m |
| 19 | 重算全部檔案 SHA-256，確認五個 frozen path 未變 | 命令輸出 | §9.3 五個逐字未變 | `sha256sum` | 17 | 3m |
| 20 | Git 範圍核對 | 命令輸出 | 只有 §9.1～§9.2；無 R04 path | §11.4 | 19 | 3m |
| 21 | 撰寫「報告 007 證據更正」獨立小節 | 本檔 §二 | 含 frozen 原文、行號、正確值、取得命令、防重犯措施 | 目視對照 §8.3 | 2 | 10m |
| 22 | 補完報告其餘章節 | 本檔 | §12.2 的 14 項 raw output 齊備 | 目視逐項對照 | 20,21 | 20m |
| 23 | 機器計算 checklist total／checked／unchecked | 命令輸出 | 相加一致 | `rg -c` | 22 | 3m |
| 24 | **全文搜尋**測試數、錯誤數、`PASS`、`完整`、`全部`、base／head 敘述，逐項對照 raw output | 命令輸出 | 每處皆有對應輸出 | `rg -n` | 23 | 8m |
| 25 | 逐欄檢查表頭，確認無「待補」 | 本檔表頭 | `rg '待補'` 僅出現在 §二 的 frozen 引用中 | `rg -n` | 24 | 2m |
| 26 | 確認待決事項；若有任何待決則不得宣告 COMPLETE | 本檔 §七 | 明確結論 | 目視 | 25 | 2m |
| 27 | **最後**寫 `REPORT-COMPLETE`，其後單一 LF，寫入後不再修改 | 本檔 | `od` 顯示 marker 後單一 `0a` | `tail -c 32 \| od` | 26 | 3m |

**預估合計：約 2 小時 20 分。** 依前六輪校準（實際／估計比 0.29–0.50），實際預期約 45–70 分鐘。

### 0.3 停止條件（檢查報告 §13 的十一項）

我特別註記最可能觸發的四項：

| 編號 | 事項 | 處理 |
|---|---|---|
| P-1 | 新增 `py.typed` 後**正常 mypy 仍有任何 error** | 立即停止（§13 第 1 項） |
| P-2 | mypy 顯示 `hotstock.domain` strict override **未使用** | 立即停止（§13 第 2 項）。這代表 override 沒真的生效，等同假通過 |
| P-3 | context 封裝無法同時滿足 fresh deep copy 與 JSON 序列化 | 立即停止（§13 第 4 項） |
| P-4 | wheel 未含 `hotstock/py.typed` | 立即停止（§13 第 6 項） |

停止時：報告標 `BLOCKED`，完整交代進度、觸發原因、為何需決策、選項、影響、建議與核准後步驟，最後仍寫 marker。

### 0.4 風險與退路

| 編號 | 風險 | 步驟 | 退路 |
|---|---|---|---|
| R-1 | 新測試未先對舊實作失敗，無法證明真的擋住漏洞 | 8 | §6.3 明訂「必須先對舊實作失敗」。我在改 `errors.py` **之前**先跑一次新測試確認 FAIL，保存輸出 |
| R-2 | 改 `errors.py` 時不慎放寬既有 JSON-safe／NaN／secret 行為 | 6,7 | §6.2 第 7 項明訂不得放寬。既有 44 tests 必須全數保持通過（步驟 9 驗證） |
| R-3 | 淺拷貝導致巢狀 list／dict 仍被污染 | 7 | §6.2 明訂不得用淺拷貝。採 JSON round-trip 產生深層複本；步驟 8 有巢狀 mutation 測試 |
| R-4 | 改 docstring 時誤動 `test_pit_metadata.py` 的測試行為 | 10 | 只改 module docstring；步驟 15 確認仍為 25 tests |
| R-5 | 五個 frozen path 雜湊意外漂移 | 全部 | 步驟 19 逐一重算比對；漂移即觸發 §13 第 7 項停止 |
| R-6 | wheel build 在 repo 產生 `dist/` | 17 | §11.3 明訂輸出到 `/tmp`；步驟 20 以 `git status` 確認 repo 無 `dist/` |

### 0.5 本輪明確不做（檢查報告 §9.4）

- 不修改 `pyproject.toml`、`uv.lock`、`scripts/check.sh`
- 不修改 enum、`PitMetadata` 或 public export
- 不新增 dependency
- 不修改任何舊工作報告或檢查報告（含報告 007）
- 不碰舊新聞工具
- 不建立任何 R04 path
- 不 commit、不 push
- 不使用 `MYPYPATH`／`PYTHONPATH`／`--explicit-package-bases` 作為正式驗證

---

## 一、Checklist

> 原樣複製自**更正報告** `20260802-194438_B0-R03-FIX1指南更正_review.md` §5 的 28 項有效工作表。
> 依該報告 §7，更正報告優先於被更正報告；被更正報告 §10 中順序矛盾的版本不再複製。
> 未完成項目**保留 `[ ]` 並說明原因，不刪除**。

- [x] 1. 確認本檔與被更正報告最後一行都是 `REVIEW-COMPLETE`。
- [x] 2. 記錄 branch、base `72d3cc059b41e7f59e42e57c5cb123a12fc3646e`、Head `uncommitted` 與起始狀態。
- [x] 3. 確認工作報告 007 SHA-256 仍是 `2b490ec79352f7b9775f60e2da4df0ecc40958e85324af32dd466a54a78da899`。
- [x] 4. 確認被更正報告 SHA-256 仍是 `39bcb65fde36ff5b210b24b3a4aa19447426740b9d2c688a52ad390197c55ac8`。
- [x] 5. 建立工作報告 008，先寫規劃；表頭從一開始就寫 `Head commit | uncommitted`。
- [x] 6. 新增 0-byte、mode 644 的 `src/hotstock/py.typed`。
- [x] 7. 驗證 editable package 可定位空 marker。
- [x] 8. 修改 `errors.py`，把 context 保存為私有不可變 snapshot。
- [x] 9. 讓 `context` property 與每次 `to_dict()` 回傳 fresh deep copy。
- [x] 10. 在 `test_errors.py` 新增原 dict、property 回傳值與 `to_dict()` 回傳值的 mutation 回歸測試。
- [x] 11. 執行 error targeted tests，保存實際 collected／passed 與 exit code。
- [x] 12. 只修改 `test_pit_metadata.py` module docstring，將依據改為 SDD，測試行為不變。
- [x] 13. 搜尋新 domain 程式與測試，確認沒有 legacy 新聞工具引用。
- [x] 14. 對工程師修改檔逐一做 whitespace check，確認沒有診斷。
- [x] 15. 執行 Ruff format check 與 lint。
- [ ] 16. 執行未加環境變數的無快取 mypy，確認 exit 0 且 domain strict override 生效。
      → **未通過。** exit 2，`Source file found twice under different module names`。見 §六 B-1。
- [x] 17. 執行全部 domain tests。
- [ ] 18. 執行完整 `./scripts/check.sh`，確認六段皆執行且整體 exit 0。
      → **未通過。** 停在 5/6，`GATE_EXIT=2`，6/6 未執行。
- [x] 19. 離線建立 wheel，確認 wheel 內含 0-byte `hotstock/py.typed`。
- [x] 20. 執行 `uv lock --check` 並確認 lockfile 無 diff。
- [x] 21. 確認 `__init__.py`、`enums.py`、`models.py`、`test_enums.py` 與報告 007 雜湊未變。
- [x] 22. 確認 Git 範圍只有前一份報告 §9 允許的 path，沒有 R04。
- [x] 23. 在報告 008 更正報告 007 的 `Head commit | 待補`，並揭露本檔所載 frozen whitespace。
- [x] 24. 機器計算 checklist total／checked／unchecked並核對摘要。
- [x] 25. 全文 residual search，逐項核對測試數、錯誤數、PASS、完整、全部與 base/head。
- [x] 26. 完成八節報告，明寫本輪修改、既有檔、審查者新增檔、同時修改與待決事項。
- [x] 27. 確認 R03 未 commit、未 push，B0-R04 未開始。
- [x] 28. 最後才寫 `REPORT-COMPLETE`，停止等待下一份檢查報告。

**計數：28 項，26 項已勾選，2 項未勾選**（26 + 2 = 28，相加一致；計數命令見 §四 4.11）。

---

## 二、報告 007 證據更正

> 被更正對象為**已凍結**的工作報告 007，SHA-256 `2b490ec79352f7b9775f60e2da4df0ecc40958e85324af32dd466a54a78da899`（§四 4.9 附驗證）。**報告 007 未被本輪修改，也不得修改。**

### 2.1 更正：表頭 `Head commit | 待補`（R03-F04）

| 欄位 | 內容 |
|---|---|
| 原位置 | 工作報告 007 **第 13 行**（表頭） |
| **原文（錯誤，已更正）** | `Head commit \| 待補` |
| **正確值** | `uncommitted` |
| 佐證位置 | 同報告第 354 行已寫 `Head commit \| uncommitted`，表頭與後文自相矛盾 |

**取得正確值的命令與輸出：**

```console
$ git rev-parse HEAD
72d3cc059b41e7f59e42e57c5cb123a12fc3646e      ← R02 closure commit，非 R03 的 head

$ git status --short
（R03 檔案皆為未追蹤，無已追蹤檔變更）
```

**說明：** R03 依 §13 尚未 commit，因此 `HEAD` 仍指向 R02 closure commit，**R03 本身沒有 head commit，正確狀態是 `uncommitted`**。

**錯在哪：** 我在建立報告 007 時把表頭填成「待補」，打算收尾時補上。收尾時只更新了「狀態」與「耗時」兩欄，**沒有逐欄檢查表頭**，因此「待補」殘留。這與 R02 已被指出並要求固定的寫法相同，屬**重犯**。

**防重犯措施（本輪已落實）：**

1. 報告 008 的表頭**自建立時即寫 `uncommitted`**，不先填「待補」再期待收尾修改（規劃 §0.2 步驟 1 與表頭下方註記）。
2. 規劃 §0.2 新增**步驟 25「逐欄檢查表頭，確認無『待補』」**，作為 marker 前的獨立關卡。
3. 驗證見 §四 4.10。

### 2.2 揭露：被更正檢查報告的 frozen whitespace 診斷

依更正報告 §4.2 要求如實揭露：

```console
$ git diff --no-index --check /dev/null docs/reviews/member-b/20260802-193902_B0-R03決策核准與FIX1指南_review.md
docs/reviews/member-b/20260802-193902_B0-R03決策核准與FIX1指南_review.md:294: trailing whitespace.
```

依更正報告 §4.2，**這是審查者造成的報告品質問題，不是工程師造成**；該報告保持原 SHA-256，我**未替審查者刪除該空白**，也未建立 ignore 或修改工具設定。

---

## 三、本輪變更

### 3.1 新增

| 檔案 | 內容 |
|---|---|
| `src/hotstock/py.typed` | 0 bytes、mode 644 的 PEP 561 標記（專案經理已核准） |
| `docs/工作報告/008_...md` | 本檔 |

### 3.2 修改

| 檔案 | 新 SHA-256 | 變更 |
|---|---|---|
| `src/hotstock/domain/errors.py` | `6fa315e48a32ec126827d0db239e7c444ed111f33a1bd60571ef980e70b432c2` | `_validate_json_safe()` 改回傳 JSON 字串；context 改存私有 `_context_json`；新增唯讀 `context` property；`to_dict()` 每次產生全新結構 |
| `tests/unit/domain/test_errors.py` | `2200cc7c152644ef136e3c22da030f77749c6725cfdfddb33b645fae50c3b3d2` | 新增 6 個建構後 mutation 回歸測試；既有 44 個未改 |
| `tests/unit/domain/test_pit_metadata.py` | `b13cbf743b854b4742c519f79a166f410a39047bbf74f0db02d21cbf02c036e3` | **只改 module docstring**，依據改為 SDD DD-013、§7.1 至 §7.2；25 個測試行為未動 |

### 3.3 報告 007 已有的實作（本輪未動）

12 個 enum、`PitMetadata`、`domain/__init__.py` 的 public export、`test_enums.py` 的 53 個測試。四者雜湊經驗證未變（§四 4.9）。

### 3.4 審查者新增、工程師不得修改的檔案

| 檔案 | 說明 |
|---|---|
| `docs/reviews/member-b/20260802-193902_..._review.md` | FIX1 指南（被更正報告） |
| `docs/reviews/member-b/20260802-194438_..._review.md` | **本輪執行期間新出現的指南更正報告** |

第二份是在我已依第一份開工後才出現的。依其 §1「不必回退、不必重做」與 §7「本更正報告優先」，我讀完後改用其 §5 的 28 項有效工作表收尾，未回退既有工作。

### 3.5 他人同時修改

**無**（`docs/reviews/` 的新增屬審查者流程，見 §3.4，非對我工作檔的同時修改）。

### 3.6 未觸碰

`pyproject.toml`、`uv.lock`、`scripts/check.sh`、`domain/__init__.py`、`enums.py`、`models.py`、`test_enums.py`、報告 007、任何既有檢查報告、舊新聞工具、任何 R04 path。

---

## 四、驗證證據

### 4.1 `py.typed`（§11.1）

```console
$ test -f src/hotstock/py.typed        regular file ✅
$ test ! -s src/hotstock/py.typed      0 bytes ✅
$ stat -c '%a %s %n' src/hotstock/py.typed
644 0 src/hotstock/py.typed

$ uv run --frozen python -c "from importlib.resources import files; p = files('hotstock').joinpath('py.typed'); print(p.is_file(), p.read_bytes())"
True b''
```

與 §11.1 預期 `644 0` 與 `True b''` 完全一致。

### 4.2 漏洞證據：修改前的舊實作確實失敗

依更正報告 §2.2，工程師可採任一順序且**不得捏造執行順序**。以下是我**實際執行**的紀錄——新測試在修改 `errors.py` **之前**先跑過一次：

```console
$ uv run --frozen pytest tests/unit/domain/test_errors.py -q     ← errors.py 尚未修改
FAILED tests/unit/domain/test_errors.py::test_mutating_returned_context_root_has_no_effect
FAILED tests/unit/domain/test_errors.py::test_mutating_returned_context_nested_has_no_effect
FAILED tests/unit/domain/test_errors.py::test_secret_cannot_be_injected_after_construction
FAILED tests/unit/domain/test_errors.py::test_mutating_to_dict_payload_has_no_effect
FAILED tests/unit/domain/test_errors.py::test_each_to_dict_returns_fresh_objects
FAILED tests/unit/domain/test_errors.py::test_each_context_access_returns_fresh_object
6 failed, 44 passed in 0.06s
PRE_FIX_EXIT=1
```

**6 個新測試全部對舊實作失敗，既有 44 個全部通過。** 這與更正報告 §2.2 保存的審查者探針結論一致。

另外，修改前也直接重現了審查者 §6.1 的 repro：

```console
{'error_code': 'DATA_QUALITY', 'message': 'safe', 'context': {'rows': [1, 2], 'api_token': 'SECRET'}}
```

### 4.3 修正後：error targeted test（§5 項目 11）

```console
$ uv run --frozen pytest tests/unit/domain/test_errors.py -q
..................................................                       [100%]
50 passed in 0.05s
ERROR_TEST_EXIT=0
```

collected/passed = **50 / 50**（44 既有 + 6 新增），**既有測試無退步**。

漏洞已封堵，同一 repro 的輸出：

```console
{'error_code': 'DATA_QUALITY', 'message': 'safe', 'context': {'rows': [1]}}
```

`api_token` 未進入，被追加的 `2` 也未污染。

封裝驗證：

```console
context 是 property: True
公開可變屬性: ['error_code', 'message']
```

**沒有任何公開的可變 context 屬性。**

### 4.4 legacy reference 搜尋（§5 項目 13）

```console
$ rg -n '舊新聞|新聞工具|run_news|fetched_at|max\(published_at' src/hotstock/domain tests/unit/domain
（無輸出）  exit=1 → 無命中
```

> `rg` 的 exit 1 代表「無符合項」，是本項期望結果，非命令失敗。

### 4.5 whitespace check（§4.3）

判準是**沒有 `--check` 診斷**；新增檔與 `/dev/null` 有內容差異時 exit 1 屬預期。

```console
✅ 無診斷 (exit 1)  py.typed
✅ 無診斷 (exit 1)  errors.py
✅ 無診斷 (exit 1)  test_errors.py
✅ 無診斷 (exit 1)  test_pit_metadata.py
✅ 無診斷 (exit 1)  008_2026-08-02_B0-R03-FIX1型別標記與context封裝.md
```

### 4.6 Ruff（§5 項目 15）

```console
$ uv run --frozen ruff format --check src/hotstock/domain tests/unit/domain
7 files already formatted        exit=0
$ uv run --frozen ruff check src/hotstock/domain tests/unit/domain
All checks passed!               exit=0
```

> 過程中曾出現 1 個 `RUF002`，來自我在新 docstring 寫的 `～`（U+FF5E 全形波浪號）。該字元**不在核准的四字元白名單**，故我改寫文字為「§7.1 至 §7.2」，**未擴張白名單**。詳見 §六 V-1。

### 4.7 mypy（§5 項目 16）——**未通過**

```console
$ uv run --frozen mypy --no-incremental src/hotstock/domain
src/hotstock/domain/enums.py: error: Source file found twice under different
  module names: "src.hotstock.domain.enums" and "hotstock.domain.enums"
src/hotstock/domain/enums.py: note: Common resolutions include:
  a) adding `__init__.py` somewhere,
  b) using `--explicit-package-bases` or adjusting `MYPYPATH`
Found 1 error in 1 file (errors prevented further checking)
exit=2
```

**這是加入 `py.typed` 之後才出現的新錯誤**，與報告 007 的 4 個 `[import-untyped]` 不同。根因分析見 §六 B-1。

### 4.8 全部 domain tests 與完整 gate（§5 項目 17–18）

```console
$ uv run --frozen pytest tests/unit/domain -q
........................................................                 [100%]
128 passed in 0.07s
```

53（enum）+ 50（error）+ 25（PIT）= **128**，與分項加總一致。

```console
$ ./scripts/check.sh
▶ 1/6 lockfile 漂移檢查    PASS
▶ 2/6 shell 語法檢查       PASS
▶ 3/6 format 檢查          PASS
▶ 4/6 lint                 PASS
▶ 5/6 型別檢查             **FAIL**
（6/6 測試未執行）
GATE_EXIT=2
```

**六段中只有五段被執行。** 我不以「測試單獨跑得過」宣稱 gate 通過。

### 4.9 wheel、lockfile 與 frozen 雜湊（§5 項目 19–21）

```console
$ uv build --offline --wheel --out-dir /tmp/hotstock-r03-wheel.XXXXXX
Successfully built hotstock_tw-0.1.0-py3-none-any.whl

$ unzip -l <wheel> | rg 'hotstock/py\.typed'
        0  2020-02-02 00:00   hotstock/py.typed        ← 恰一個，0 bytes

$ unzip -p <wheel> hotstock/py.typed | wc -c
0

$ ls -d dist
ls: 無法存取 'dist': 沒有此一檔案或目錄        ← repo 未產生 dist/

$ uv lock --check                                exit 0
$ git diff --exit-code HEAD -- uv.lock           exit 0（無 diff）
```

**§9.3 五個 frozen path 雜湊逐一比對：**

| 檔案 | 結果 |
|---|:---:|
| `src/hotstock/domain/__init__.py` | ✅ 未變 |
| `src/hotstock/domain/enums.py` | ✅ 未變 |
| `src/hotstock/domain/models.py` | ✅ 未變 |
| `tests/unit/domain/test_enums.py` | ✅ 未變 |
| 工作報告 007 | ✅ 未變（`2b490ec7…`） |

被更正檢查報告雜湊：`39bcb65fde36ff5b210b24b3a4aa19447426740b9d2c688a52ad390197c55ac8` ✅ 與更正報告 §5 項目 4 預期相符。

### 4.10 表頭逐欄檢查（§0.2 步驟 25）

```console
$ rg -n '待補' docs/工作報告/008_2026-08-02_B0-R03-FIX1型別標記與context封裝.md
```

僅在 §二 2.1 的 frozen 引用中出現（描述報告 007 的錯誤原文），**本檔表頭無「待補」**。

### 4.11 本報告 checklist 機器計數（§5 項目 24）

```console
$ sed -n '/^## 一、Checklist/,/^\*\*計數/p' 008_...md | rg -c '^- \[x\]'
26
$ sed -n '/^## 一、Checklist/,/^\*\*計數/p' 008_...md | rg -c '^- \[[x ]\]'
28
```

total = 28、checked = 26、unchecked = 2，**26 + 2 = 28** 相加一致。

---

## 五、Git 證據

| 項目 | 值 |
|---|---|
| Branch | `feature/b0-skeleton` |
| Base commit | `72d3cc059b41e7f59e42e57c5cb123a12fc3646e` |
| **Head commit** | **uncommitted** |
| `git diff --name-only 72d3cc0` | 空——**無任何已追蹤檔被修改** |
| `git diff --check` | exit 0 |

**`git status --short --untracked-files=all`（12 個未追蹤檔）：**

```text
?? docs/reviews/member-b/20260802-193902_..._review.md        ← 審查者
?? docs/reviews/member-b/20260802-194438_..._review.md        ← 審查者
?? docs/工作報告/007_...md                                     ← 報告 007（frozen）
?? docs/工作報告/008_...md                                     ← 本檔
?? src/hotstock/domain/__init__.py
?? src/hotstock/domain/enums.py
?? src/hotstock/domain/errors.py
?? src/hotstock/domain/models.py
?? src/hotstock/py.typed                                       ← 本輪新增
?? tests/unit/domain/test_enums.py
?? tests/unit/domain/test_errors.py
?? tests/unit/domain/test_pit_metadata.py
```

全部落在前一份報告 §9 允許範圍內（含審查者自己新增的兩份檢查報告）。

```console
$ test ! -e src/hotstock/data && test ! -e src/hotstock/adapters && test ! -e src/hotstock/cli.py
  ✅ 三個 R04 path 皆不存在
```

未執行 commit、push、merge、rebase、amend、squash。

---

## 六、問題、偏差與風險

### B-1　【阻塞】加入 `py.typed` 後 mypy 出現模組名稱衝突

**現象：**

```text
error: Source file found twice under different module names:
       "src.hotstock.domain.enums" and "hotstock.domain.enums"
```

**根因：** 兩個條件同時成立才觸發——

1. `pyproject.toml` 的 `[tool.mypy] files = ["src/hotstock"]`（第 53 行）以**檔案路徑**指定目標。
2. repo 內存在 **legacy 的 `src/__init__.py`**（0 bytes，git 已追蹤，屬舊新聞工具遺留），使 `src/` 本身成為一個 Python package。

因此 mypy 從路徑推導出模組名 `src.hotstock.domain.enums`；而 `py.typed` 加入後，同一檔案又可經 editable 安裝解析為 `hotstock.domain.enums`。**同檔兩名**，mypy 直接中止。

**診斷實測（僅供診斷，未寫入正式流程，比照更正報告 §4.2 的作法）：**

| 診斷 | 命令 | 結果 |
|---|---|---|
| A | `mypy --no-incremental -p hotstock` | **exit 0**，`unused section(s)` 只列 `hotstock.data`，**`hotstock.domain` 不在其中 → strict override 生效** |
| B | `MYPYPATH=src mypy --explicit-package-bases src/hotstock` | exit 0，同上 |
| C | 暫時移開 `py.typed` 後重跑 | 回到 4 個 `[import-untyped]` → **`py.typed` 確為必要** |
| D | 暫時移開 legacy `src/__init__.py` 後重跑 | **exit 0**，`hotstock.domain` 不在 unused 清單 |

> 診斷 C、D 皆為**暫時移開後立即還原**，並以 SHA-256 驗證還原無誤（`src/__init__.py` 還原後 hash 與移開前一致，`git status` 對該檔無輸出）。`py.typed` 現為 `644 0`。

**為什麼我不能自行解決：**

| 修法 | 阻礙 |
|---|---|
| `scripts/check.sh` 的 `mypy src/hotstock` → `mypy -p hotstock` | **更正報告 §6 明訂 `scripts/check.sh` 不得修改** |
| `pyproject.toml` 的 `files = ["src/hotstock"]` → `packages = ["hotstock"]` | **§6 明訂 `pyproject.toml` 不得修改** |
| 刪除 legacy `src/__init__.py` | **屬舊新聞工具，DEC-003 明訂不主動刪除；§9.4 亦禁止碰舊新聞工具** |
| 正式命令加 `MYPYPATH`／`--explicit-package-bases` | **§11.2 明文禁止** |
| 移除 `py.typed` | 回到 4 個 `[import-untyped]`（診斷 C），且違反已核准決策 |

觸發停止條件 §13 第 1 項（「新增 `py.typed` 後，正常 mypy 仍有任何 error」）與第 3 項（「需要修改 `pyproject.toml`、`uv.lock` 或 import 策略」）。

**選項與建議見 §七 Q-1。**

### V-1　一個 RUF002：改寫自己的文字，未擴張白名單

我在 `test_pit_metadata.py` 新 docstring 寫了 `§7.1～§7.2`，其中 `～`（U+FF5E 全形波浪號）**不在核准的四字元白名單**（`，：（）`）。

依 R02 檢查報告 §2.1「不加入未實際觀測的字元」，我**改寫文字**為「§7.1 至 §7.2」，未動白名單。這與 R03 處理 `；`、`／` 的作法一致。

### V-2　新測試曾需 `ruff format` 重排

首次 format check 對 `test_errors.py` 回報兩處超長行。執行 `ruff format` 後通過（§四 4.6）。**格式化只作用於本輪授權的變更檔。**

### V-3　執行期間出現新的檢查報告

我依 `20260802-193902` 開工後，`20260802-194438` 指南更正報告才出現。依其 §1「不必回退、不必重做」與 §7「本更正報告優先」，我讀完後改用其 §5 的 28 項工作表收尾，**未回退既有工作**。

順帶說明：更正報告 §2 放寬了「測試須先於實作」的要求，但我**實際上確實先寫測試並跑出 red**（§四 4.2），該證據為真實執行紀錄，非事後補述。

### V-4　工時：估 2h20m，實際約 55 分鐘

| 輪次 | 估計 | 實際 | 比值 |
|---|---:|---:|---:|
| R00 | 3h | 1.5h | 0.50 |
| R01 | 3h25m | 1h | 0.29 |
| R02 | 3h35m | 1h10m | 0.33 |
| R02-FIX1 | 88m | 35m | 0.40 |
| R02-FIX2 | 35m | 15m | 0.43 |
| R03 | 3h48m | 75m | 0.33 |
| **R03-FIX1** | **2h20m** | **約 55m** | **0.39** |

### 範圍外變更

**無。** 新增 2 檔、修改 3 檔，全部落在更正報告 §6 的允許清單內。

---

## 七、待決事項

### Q-1　【阻塞】如何解除 mypy 模組名稱衝突

**目前做到哪裡：** B0-R03-FIX1 的四項工作中，三項已完成——`py.typed` 已建立並驗證（含 wheel 內含 0-byte marker）、error context 漏洞已封堵（6 個新回歸測試，50 passed）、測試設計依據已收回 SDD。**唯一未完成的是型別檢查通過。**

**觸發停止的具體事件：** 加入已核准的 `py.typed` 後，正常 mypy 從原本的 4 個 `[import-untyped]` 變成 1 個 `Source file found twice under different module names`（exit 2）。

**為何超出授權：** 四條可行修法分別落在 `scripts/check.sh`、`pyproject.toml`、legacy `src/__init__.py` 與被禁止的環境變數上，**全部在不得修改清單內**。

| 選項 | 作法 | 影響 | 評估 |
|---|---|---|---|
| **A（建議）** | `scripts/check.sh` 第 37 行：`mypy src/hotstock` → `mypy -p hotstock` | 改動最小（單行），不動 `pyproject.toml`、不動 legacy。診斷 A 實測 exit 0 且 strict override 生效 | **需授權修改 `scripts/check.sh`**。另需同步更新檢查報告 §11.2 的 targeted 命令 |
| B | `pyproject.toml`：`[tool.mypy] files = ["src/hotstock"]` → `packages = ["hotstock"]` | 語意上更正確（本專案已安裝為 package），且 targeted 與 gate 命令都會受益 | 需授權修改 `pyproject.toml` |
| C | 刪除 legacy `src/__init__.py` | 診斷 D 實測有效。該檔是舊 flat layout 遺留，在 src-layout 下本就多餘 | **風險：`run_news.py` 有 `from src.pipeline import run`，刪除可能影響舊工具。** 且受 DEC-003 保護，需專案經理明確授權 |
| D | 正式命令加 `MYPYPATH`／`--explicit-package-bases` | — | **§11.2 明文禁止**，不建議 |

**我的建議是 A。** 理由：改動範圍最小（單行）、不觸碰 legacy 檔案、不改變依賴或專案 metadata，且 `-p hotstock` 對已安裝 package 而言是 mypy 的標準用法。若同時希望 targeted 命令一致，可一併把檢查報告 §11.2 的 `mypy --no-incremental src/hotstock/domain` 改為 `mypy --no-incremental -p hotstock`。

**核准後的精確下一步：** 依授權修改單一檔案 → 重跑 `mypy --no-incremental`（確認 exit 0 且 `hotstock.domain` 不在 unused 清單）→ 重跑完整 `check.sh`（確認六段皆執行、exit 0）→ 新增工作報告 009 → 停止等待檢查。

**仍然禁止：** 不改 `uv.lock`、不改 import 策略、不碰 legacy 新聞工具、不加環境變數、不開始 R04、不 commit、不 push。

---

## 八、給審查者的重現步驟

```bash
cd ~/projects/hotstock-tw

# 1. 基線
git rev-parse HEAD                       # 72d3cc0...
git status --short --untracked-files=all
git diff --name-only 72d3cc0             # 空

# 2. py.typed
stat -c '%a %s %n' src/hotstock/py.typed # 644 0
uv run --frozen python -c "from importlib.resources import files; p=files('hotstock').joinpath('py.typed'); print(p.is_file(), p.read_bytes())"

# 3. 重現阻塞（預期 exit 2）
rm -rf .mypy_cache
uv run --frozen mypy --no-incremental src/hotstock/domain; echo "exit=$?"
./scripts/check.sh; echo "GATE_EXIT=$?"

# 4. 驗證建議方案 A（僅診斷，勿寫入流程）
uv run --frozen mypy --no-incremental -p hotstock; echo "exit=$?"
#   預期 exit 0，unused section 只有 hotstock.data

# 5. context 漏洞已封堵
uv run --frozen python -c "
from hotstock.domain.errors import ErrorCode, HotstockError
e = HotstockError(ErrorCode.DATA_QUALITY, 'safe', {'rows': [1]})
e.context['api_token'] = 'SECRET'
p = e.to_dict(); p['context']['rows'].append(2)
print(e.to_dict())"
#   預期 {'error_code': 'DATA_QUALITY', 'message': 'safe', 'context': {'rows': [1]}}

# 6. 測試
uv run --frozen pytest tests/unit/domain/test_errors.py -q       # 50 passed
uv run --frozen pytest tests/unit/domain -q                      # 128 passed
uv run --frozen ruff format --check src/hotstock/domain tests/unit/domain
uv run --frozen ruff check src/hotstock/domain tests/unit/domain

# 7. legacy 搜尋（預期 exit 1 無命中）
rg -n '舊新聞|新聞工具|run_news|fetched_at|max\(published_at' src/hotstock/domain tests/unit/domain

# 8. wheel
d=$(mktemp -d /tmp/hs.XXXXXX); uv build --offline --wheel --out-dir "$d"
unzip -l "$d"/*.whl | rg 'hotstock/py\.typed'
unzip -p "$d"/*.whl hotstock/py.typed | wc -c    # 0
rm -rf "$d"; ls -d dist 2>&1                      # No such file

# 9. frozen 雜湊
sha256sum src/hotstock/domain/__init__.py src/hotstock/domain/enums.py \
  src/hotstock/domain/models.py tests/unit/domain/test_enums.py \
  "docs/工作報告/007_2026-08-02_B0-R03共用enum錯誤與PIT基礎.md" \
  "docs/reviews/member-b/20260802-193902_B0-R03決策核准與FIX1指南_review.md"

# 10. checklist 計數
sed -n '/^## 一、Checklist/,/^\*\*計數/p' "docs/工作報告/008_2026-08-02_B0-R03-FIX1型別標記與context封裝.md" | rg -c '^- \[x\]'     # 26
sed -n '/^## 一、Checklist/,/^\*\*計數/p' "docs/工作報告/008_2026-08-02_B0-R03-FIX1型別標記與context封裝.md" | rg -c '^- \[[x ]\]'  # 28

# 11. marker
tail -c 32 "docs/工作報告/008_2026-08-02_B0-R03-FIX1型別標記與context封裝.md" | od -An -tx1
```

**預期：** 步驟 3 兩者皆非零；步驟 4 exit 0；步驟 5 無 `api_token`；步驟 6 依序 50／128 且 Ruff 皆 exit 0；步驟 7 exit 1；步驟 9 五個 frozen 雜湊與 §四 4.9 相符；步驟 10 依序 26／28。

---

## 九、停止聲明

**B0-R03-FIX1 未完成，狀態 BLOCKED，工作已停止。**

- 阻塞於 §七 Q-1，**需授權才能解除**（建議修改 `scripts/check.sh` 單行）。
- **未自行採用任何未授權方案**——未改 `pyproject.toml`、未改 `scripts/check.sh`、未刪 legacy `src/__init__.py`、未在正式命令加環境變數、未移除已核准的 `py.typed`。
- 四項工作中三項已完成：`py.typed`（含 wheel 驗證）、context 封裝（6 個新回歸測試，50 passed）、測試依據收回 SDD。
- **完整 gate 未通過**，停在 5/6，第 6 段在 gate 內未被執行。我不以「測試單獨跑得過」宣稱 gate 通過。
- 報告 007 的 `Head commit | 待補` 已於 §二 正式更正；報告 007 本身未被修改（雜湊已驗證）。
- **未 commit。** R03 須待檢查報告判定 PASS 後才收 commit。
- 未 push、merge、rebase、amend、squash。
- **B0-R04 尚未開始。**
- 等待 `docs/reviews/member-b/` 中新的檢查報告。

<!-- REPORT-COMPLETE -->
