# 工作報告 007｜B0-R03 共用 enum、error 與 PIT 基礎

| 項目 | 內容 |
|---|---|
| 日期 | 2026-08-02 |
| 輪次 | 007（**B0-R03**） |
| 狀態 | **BLOCKED** |
| 阻塞原因 | 完整 gate 停在 5/6 型別檢查：mypy 回報 4 個 `[import-untyped]`。修法需新增 `src/hotstock/py.typed`，該檔**不在 §7.2 授權清單**。觸發 §14 停止條件第 7 項。詳見 §六 Q-1 |
| 耗時 | 約 75 分鐘 |
| Branch | `feature/b0-skeleton` |
| **R02 closure commit** | `72d3cc059b41e7f59e42e57c5cb123a12fc3646e` |
| **Base commit（R03_BASE）** | 同上 |
| Head commit | 待補 |
| 起始 `git status --short` | **空（工作區乾淨）** |
| 對應檢查報告 | `docs/reviews/member-b/20260802-190210_B0-R02證據更正通過與R03解鎖_review.md` §7–§15 |

---

## 〇、本輪規劃步驟

> 本節於執行前寫定。執行中若發現規劃有誤，**不修改本節**，一律記入「五、問題、偏差與風險」。

### 0.1 唯一目標

建立低耦合、不可變、可序列化、可由組員 A 直接依賴的**最小 Domain 基礎**：12 個共用 enum、SDD §24.1 七類錯誤、雙 PIT metadata，以及對上述契約的單元測試。

### 0.2 步驟表

> 工時估計依前五輪校準（實際／估計比 0.29–0.50）直接下修填寫。

| # | 動作 | 產出檔案 | 完成判準 | 驗證方式 | 依賴 | 預估 |
|---:|---|---|---|---|---|---:|
| 1 | 記錄 base、branch、乾淨起始狀態 | 本檔表頭 | base = `72d3cc0`，起始 status 為空 | `git rev-parse HEAD`／`git status --short` | — | 2m |
| 2 | 寫定本規劃並原樣複製 §12 的 28 項清單 | 本檔 §〇、§一 | 28 項逐字複製，未增減 | 目視對照檢查報告 §12 | 1 | 12m |
| 3 | 建立無副作用的 `domain/__init__.py`（先空殼，§17 再補 export） | `src/hotstock/domain/__init__.py` | 可 import，無副作用 | 步驟 19 | 2 | 3m |
| 4 | 實作 `enums.py` 的 12 個 `StrEnum` | `src/hotstock/domain/enums.py` | 逐字符合 §8 表格；無 `auto()`、無 alias | 步驟 5–6 | 3 | 15m |
| 5 | 實作 `test_enums.py`（§11.1 七項） | `tests/unit/domain/test_enums.py` | 12 個 enum 全量比對 member 與 value，非抽驗 | 步驟 6 | 4 | 20m |
| 6 | 只跑 enum targeted test | 命令輸出 | 保存命令、exit code、collected／passed 數 | `pytest tests/unit/domain/test_enums.py` | 5 | 3m |
| 7 | 實作 `errors.py`：七值 `ErrorCode` + 安全 JSON context 驗證 | `src/hotstock/domain/errors.py` | 七值逐字符合 §9.1；context 拒絕非 JSON-safe 值 | 步驟 9 | 4 | 20m |
| 8 | 完成 `HotstockError` 的 defensive copy、`str()` 不洩 context、`to_dict()` | 同上 | 符合 §9.2 全部要求 | 步驟 9 | 7 | 10m |
| 9 | 實作 `test_errors.py`（§11.2 九項） | `tests/unit/domain/test_errors.py` | 正常、邊界與拒絕測試齊備；secret 覆蓋大小寫與 `api_key` 變體 | 步驟 10 | 8 | 20m |
| 10 | 只跑 error targeted test | 命令輸出 | 保存命令、exit code、collected／passed 數 | `pytest .../test_errors.py` | 9 | 3m |
| 11 | 實作 `models.py` 的 frozen、extra-forbid `PitMetadata` | `src/hotstock/domain/models.py` | 13 欄位型別與 Null 性符合 §10.2 | 步驟 13 | 4 | 15m |
| 12 | 完成 aware datetime、Asia/Taipei 正規化與 §10.3 全部 invariant | 同上 | naive 拒絕；`system_available_from == first_seen_at`；`available_from` 依 `pit_mode` 選欄位、**無 `max()`** | 步驟 13 | 11 | 15m |
| 13 | 實作 `test_pit_metadata.py`（§11.3 十六項） | `tests/unit/domain/test_pit_metadata.py` | 雙 PIT、時區、不可變、序列化測試齊備 | 步驟 14 | 12 | 25m |
| 14 | 只跑 PIT targeted test | 命令輸出 | 保存命令、exit code、collected／passed 數 | `pytest .../test_pit_metadata.py` | 13 | 3m |
| 15 | 補完 `domain/__init__.py` 的 `__all__` 明確 export | `src/hotstock/domain/__init__.py` | 只 export 本輪核准公開型別，**不 export 私有 helper** | 步驟 19 | 3,4,7,11 | 5m |
| 16 | 只格式化本輪新檔 | — | `ruff format src/hotstock/domain tests/unit/domain` | 同左 | 15 | 2m |
| 17 | targeted Ruff、mypy 與全部 domain 測試 | 命令輸出 | 三者皆 exit 0 | §12.1 命令 | 16 | 5m |
| 18 | 完整 `./scripts/check.sh` | 命令輸出 | 六段結果、總測試數、整體 exit 0 | 同左 | 17 | 3m |
| 19 | `uv lock --check` 與 `uv.lock` 無 diff | 命令輸出 | 兩者皆通過 | 同左 | 18 | 2m |
| 20 | Git 證據：變更只落在 §7.2 授權 path，R04 paths 不存在 | 命令輸出 | `git diff --name-only R03_BASE` 僅授權 path | §12.2 命令 | 19 | 3m |
| 21 | 對照 §8／§9／§10／§11 逐條人工與機器核對 | 命令輸出 | 任何不符不得用摘要掩蓋 | 逐條 | 20 | 10m |
| 22 | 機器計算本清單 total／checked／unchecked 並驗證相加一致 | 命令輸出 | `total = checked + unchecked` | `rg -c` | 21 | 3m |
| 23 | **全文搜尋**報告中的測試數、錯誤數、`PASS`、`完整`、`全部`、base／head 陳述，逐一對照 raw output | 命令輸出 | 每處皆有對應 raw output | `rg -n` | 22 | 8m |
| 24 | 補完報告八節 | 本檔 | 明寫本輪修改／本輪前已存在／他人同時修改／偏差／待決／重現步驟 | 目視 | 23 | 20m |
| 25 | 確認 R04 未開始、R03 未 commit、未 push | 命令輸出 | 三者皆確認 | `git log`／`ls` | 24 | 2m |
| 26 | **最後**寫 `REPORT-COMPLETE`，其後單一 LF，寫入後不再修改 | 本檔 | `od` 顯示 marker 後單一 `0a` | `tail -c 32 \| od` | 25 | 3m |

**預估合計：約 3 小時 48 分。** 見 §0.4 風險 R-4。

### 0.3 決策點（觸發即依 §14 立即停止並報 BLOCKED）

檢查報告 §14 列出十項立即停止條件。我特別註記三項最可能觸發者：

| 編號 | 事項 | 處理 |
|---|---|---|
| P-1 | SDD 值與檢查報告 §8 enum 表不一致 | **立即停止**（§14 第 1 項）。不自行取捨，保留兩邊原文於報告 |
| P-2 | `PitMetadata` invariant 無法同時滿足 SDD | **立即停止**（§14 第 4 項） |
| P-3 | 需要新增套件或改 `pyproject.toml`／`uv.lock` | **立即停止**（§14 第 6 項）。Pydantic v2 已在 R01 鎖定，預期不需新增 |
| P-4 | 是否 commit R03 | **不 commit。** §13 明訂須待下一份 PASS 檢查報告 |

### 0.4 風險步驟與退路

| 編號 | 風險 | 步驟 | 退路 |
|---|---|---|---|
| R-1 | `StrEnum` 的 `Market.TPEX.value` 大小寫被誤寫成 `TPEX` | 4 | §8 明訂必須是 SDD 原文 `TPEx`；步驟 5 有專項測試，步驟 21 再人工核對 |
| R-2 | `available_from` 誤用 `max()` 合併雙 PIT——**這正是舊新聞工具的錯誤** | 12 | §10.3 明訂禁止。以 validator 依 `pit_mode` 明確選欄位；步驟 13 有專項反向測試 |
| R-3 | secret 偵測遺漏變體（如 `API_KEY`、`api-key`、`x_secret_x`） | 7 | §9.2 規定 key 小寫並移除 `-`、`_` 後做**包含**比對；步驟 9 覆蓋大小寫與 `api_key` 變體 |
| R-4 | 預估 3h48m 遠超前幾輪實際 | 全部 | 上限未指定。依 0.29–0.50 校準，實際預期約 70–110 分鐘。若真超時，**不砍測試**，據實記錄 |
| R-5 | mypy strict override 對新建的 `hotstock.domain` 觸發更嚴格檢查而報錯 | 17 | 這是預期行為（override 本就為此設定）。若報錯，修正 `domain/` 程式碼本身，**不得放寬 override**（§13） |
| R-6 | `tests/unit/domain/__init__.py` 非必要卻誤建 | 5 | §7.2 明訂「只有測試匯入確實需要時才建立」。先不建，若 pytest collection 失敗再補並記錄 |

### 0.5 本輪明確不做（檢查報告 §13）

- 不建立 `PipelineRun`，不實作 phase transition
- 不建立 CandidateCard、Candidate、ThemeFrame、ScoreResult、Label、Signal 或交易模型
- 不建立 database、migration、repository、adapter、data normalization
- 不建立 CLI、排程、設定檔或 web
- 不實作 as-of query、revision selector、manifest、leakage resolver
- 不加入依賴，不更新 lockfile
- 不修改 R02 品質規則來消除 mypy note
- 不碰舊新聞工具
- **不開始 B0-R04**
- **不提交 R03**

---

## 一、Checklist

> 原樣複製自檢查報告 §12。未完成項目**保留 `[ ]` 並說明原因，不刪除**。

- [x] 1. 確認本檢查報告最後一行是 `REVIEW-COMPLETE`，結論為 PASS，解鎖輪次確為 B0-R03。
- [x] 2. 依 §6 完成 R02 唯一本機 closure commit；確認 staged 恰為 17 paths、gate exit 0、commit 後工作區乾淨。
- [x] 3. 記錄 R03 base commit、branch 與乾淨的起始 `git status --short`。
- [x] 4. 建立工作報告 007，先寫完逐步規劃、依賴、完成判準、驗證方式、風險與明確不做，再開始程式修改。
- [x] 5. 新增無副作用的 `src/hotstock/domain/__init__.py`。
- [x] 6. 新增 `enums.py`，逐字實作 §8 的 12 個 `StrEnum`。
- [x] 7. 新增 `test_enums.py`，完成 §11.1 全量契約測試。
- [x] 8. 只執行 enum targeted test，保存命令、exit code、collected/passed 數。
- [x] 9. 新增 `errors.py` 的七值 `ErrorCode` 與安全 JSON context 驗證。
- [x] 10. 完成 `HotstockError` 的 defensive copy、無 secret 字串輸出與 `to_dict()`。
- [x] 11. 新增 `test_errors.py`，完成 §11.2 正常、邊界與拒絕測試。
- [x] 12. 只執行 error targeted test，保存命令、exit code、collected/passed 數。
- [x] 13. 新增 `models.py` 的 frozen、extra-forbid `PitMetadata`。
- [x] 14. 完成 aware datetime、Asia/Taipei 正規化與 §10.3 invariant。
- [x] 15. 新增 `test_pit_metadata.py`，完成 §11.3 的雙 PIT、時區、不可變與序列化測試。
- [x] 16. 只執行 PIT targeted test，保存命令、exit code、collected/passed 數。
- [x] 17. 完成 `domain/__init__.py` 的明確 public export 與 `__all__`。
- [x] 18. 執行 `uv run --frozen ruff format src/hotstock/domain tests/unit/domain`，只格式化本輪新檔。
- [ ] 19. 執行 targeted Ruff、mypy 與全部 domain unit tests。
      → **部分完成。** Ruff format／lint exit 0，domain 測試 122 passed；**mypy exit 1（4 個 `[import-untyped]`）**。見 §五 B-1。
- [ ] 20. 執行完整 `./scripts/check.sh`，保存六段結果、總測試數與整體 exit code。
      → **已執行但未通過。** 停在 5/6 型別檢查，`GATE_EXIT=1`，6/6 未執行。輸出見 §三 3.6。
- [x] 21. 執行 `uv lock --check` 並確認 `uv.lock` 無 diff。
- [x] 22. 以 Git 證據確認變更只落在 §7.2 授權 path，R04 paths 不存在。
- [x] 23. 對照 §8、§9、§10、§11 逐條人工與機器核對，任何不符不得用摘要掩蓋。
- [x] 24. 機器計算本清單 `total`、`checked`、`unchecked`，確認相加一致。
- [x] 25. 全文搜尋工作報告中的測試數、錯誤數、`PASS`、`完整`、`全部`、base/head 等陳述，逐一對照 raw output。
- [x] 26. 完成工作報告八節，明寫本輪修改、本輪前已存在、他人同時修改、偏差、待決事項與重現步驟。
- [x] 27. 確認 B0-R04 未開始、R03 未 commit、未 push。
- [x] 28. 最後才寫 `REPORT-COMPLETE`；標記後不再修改報告，停止並等待檢查。

**計數：28 項，26 項已勾選，2 項未勾選**（26 + 2 = 28，相加一致；計數命令見 §三 3.9）。

---

## 二、本輪變更

### 2.1 新增（全部落在 §7.2 授權清單內）

| 檔案 | 行數 | 內容 |
|---|---:|---|
| `src/hotstock/domain/__init__.py` | 45 | 無副作用；以 `__all__` 明確 export 16 個公開型別，不 export 私有 helper |
| `src/hotstock/domain/enums.py` | 134 | 12 個 `StrEnum`，逐字實作 §8 表格；無 `auto()`、無 alias |
| `src/hotstock/domain/errors.py` | 121 | 七值 `ErrorCode`、`HotstockError`、私有 JSON-safe 與密鑰驗證 helper |
| `src/hotstock/domain/models.py` | 126 | frozen／extra-forbid 的 `PitMetadata`，13 欄位 + `PROJECT_TIMEZONE` |
| `tests/unit/domain/test_enums.py` | 156 | 53 tests |
| `tests/unit/domain/test_errors.py` | 163 | 44 tests |
| `tests/unit/domain/test_pit_metadata.py` | 197 | 25 tests |
| `docs/工作報告/007_...md` | — | 本檔（自我指涉，不寫入行數） |

**`tests/unit/domain/__init__.py` 未建立**——§7.2 註明「只有測試匯入確實需要時才建立」，pytest 可正常 collect，故省略（規劃 §0.4 風險 R-6 的處置）。

### 2.2 修改

**無。** `git diff --numstat` 相對 `R03_BASE` 為空，沒有任何已追蹤檔被修改。

### 2.3 本輪前已存在

R02 closure commit `72d3cc0` 的全部內容。

### 2.4 他人同時修改

**無。**

### 2.5 未觸碰

`pyproject.toml`、`uv.lock`、`.gitignore`、`README.md`、`docs/adr/*`、`scripts/check.sh`、既有測試、既有工作報告與檢查報告、舊新聞範圍。

---

## 三、驗證證據

### 3.1 enum targeted test（步驟 8）

```console
$ uv run --frozen pytest tests/unit/domain/test_enums.py -q
.....................................................                    [100%]
53 passed in 0.02s
ENUM_TEST_EXIT=0
```

collected/passed = **53 / 53**。

### 3.2 error targeted test（步驟 12）

```console
$ uv run --frozen pytest tests/unit/domain/test_errors.py -q
............................................                             [100%]
44 passed in 0.02s
ERROR_TEST_EXIT=0
```

collected/passed = **44 / 44**。

### 3.3 PIT targeted test（步驟 16）

```console
$ uv run --frozen pytest tests/unit/domain/test_pit_metadata.py -q
.........................                                                [100%]
25 passed in 0.15s
PIT_TEST_EXIT=0
```

collected/passed = **25 / 25**。

### 3.4 全部 domain 測試

```console
$ uv run --frozen pytest tests/unit/domain -q
..................................................                       [100%]
122 passed in 0.07s
```

53 + 44 + 25 = **122**，與分項加總一致。

### 3.5 targeted Ruff（步驟 18–19）

```console
$ uv run --frozen ruff format src/hotstock/domain tests/unit/domain
2 files reformatted, 5 files left unchanged

$ uv run --frozen ruff format --check src/hotstock/domain tests/unit/domain
7 files already formatted        exit=0

$ uv run --frozen ruff check src/hotstock/domain tests/unit/domain
All checks passed!               exit=0
```

> **首次執行 lint 曾出現 6 個 RUF001／RUF002**，來自 `；`（U+FF1B 全形分號）與 `／`（U+FF0F 全形斜線）。這兩個字元**不在核准的四字元白名單**（`，：（）`），且 R02 檢查報告 §2.1 明文禁止擴張白名單。因此我**改寫自己新檔中的文字**（授權範圍內），未動白名單。詳見 §五 V-1。

### 3.6 完整 gate（步驟 20）——**未通過**

```console
$ ./scripts/check.sh
▶ 1/6 lockfile 漂移檢查    → Resolved 38 packages          PASS
▶ 2/6 shell 語法檢查       →（無輸出）                      PASS
▶ 3/6 format 檢查          → 已格式化                       PASS
▶ 4/6 lint                 → All checks passed!            PASS
▶ 5/6 型別檢查             → Found 4 errors in 2 files      **FAIL**
（6/6 測試未執行，set -e 於 5/6 中止）

GATE_EXIT=1
```

**六段中只有五段被執行，第六段未執行。** 我不把「測試單獨跑得過」當作 gate 通過。

### 3.7 mypy 完整輸出（阻塞點）

```console
$ uv run --frozen mypy src/hotstock/domain
src/hotstock/domain/__init__.py:11: error: Skipping analyzing "hotstock.domain.enums":
  module is installed, but missing library stubs or py.typed marker  [import-untyped]
src/hotstock/domain/__init__.py:25: error: Skipping analyzing "hotstock.domain.errors": ...  [import-untyped]
src/hotstock/domain/__init__.py:26: error: Skipping analyzing "hotstock.domain.models": ...  [import-untyped]
src/hotstock/domain/models.py:21: error: Skipping analyzing "hotstock.domain.enums": ...  [import-untyped]
pyproject.toml: note: unused section(s): module = ['hotstock.data', 'hotstock.data.*']
Found 4 errors in 2 files (checked 4 source files)
exit=1
```

錯誤碼統計：

```console
$ uv run --frozen mypy src/hotstock/domain 2>&1 | rg -o '\[[a-z-]+\]' | sort | uniq -c
      4 [import-untyped]
```

**四個錯誤同源。** 根因診斷：

```console
$ ls -la src/hotstock/py.typed
ls: 無法存取 'src/hotstock/py.typed': 沒有此一檔案或目錄

$ uv run --frozen python -c "import hotstock, hotstock.domain; print(hotstock.__file__)"
/home/xinyu/projects/hotstock-tw/src/hotstock/__init__.py

$ （site-packages 內）
/home/xinyu/projects/hotstock-tw/.venv/lib/python3.12/site-packages/_editable_impl_hotstock_tw.pth
/home/xinyu/projects/hotstock-tw/.venv/lib/python3.12/site-packages/hotstock_tw-0.1.0.dist-info
```

套件以 editable `.pth` 安裝。mypy 依 PEP 561 判定：套件「已安裝」但**沒有 `py.typed` 標記**，因此視為未提供型別的第三方庫而跳過分析。錯誤訊息本身即明白指出 `missing library stubs or py.typed marker`。

R02 時 `src/hotstock/` 只有 `__init__.py` 且無內部 import，未觸發此問題；本輪首次出現跨模組 import 才暴露。

### 3.8 `uv.lock` 與範圍（步驟 21–22）

```console
$ uv lock --check                                    exit 0
$ git diff --exit-code HEAD -- uv.lock               exit 0（無 diff）
$ git diff --name-only 72d3cc0                       （空，無已追蹤檔變更）
$ git diff --numstat                                 （空）
$ git diff --check                                   exit 0

$ git ls-files --others --exclude-standard
docs/工作報告/007_2026-08-02_B0-R03共用enum錯誤與PIT基礎.md
src/hotstock/domain/__init__.py
src/hotstock/domain/enums.py
src/hotstock/domain/errors.py
src/hotstock/domain/models.py
tests/unit/domain/test_enums.py
tests/unit/domain/test_errors.py
tests/unit/domain/test_pit_metadata.py
```

**恰好 8 個未追蹤檔，全部在 §7.2 授權清單內。**

```console
$ test ! -e src/hotstock/data && test ! -e src/hotstock/adapters && test ! -e src/hotstock/cli.py
  ✅ src/hotstock/data 不存在
  ✅ src/hotstock/adapters 不存在
  ✅ src/hotstock/cli.py 不存在
```

### 3.9 本報告 checklist 機器計數（步驟 24）

```console
$ sed -n '/^## 一、Checklist/,/^\*\*計數/p' 007_...md | rg -c '^- \[x\]'
26
$ sed -n '/^## 一、Checklist/,/^\*\*計數/p' 007_...md | rg -c '^- \[[x ]\]'
28
```

total = 28、checked = 26、unchecked = 2，**26 + 2 = 28** 相加一致。

### 3.10 契約逐條核對（步驟 23）

| 契約 | 核對方式 | 結果 |
|---|---|---|
| §8 十二個 enum 的 member 與 value | `test_enums.py` 的 `EXPECTED` 表逐字比對，非抽驗 | 53 tests 通過 |
| §8 無 alias | `len(__members__) == len(list(cls))` | 通過 |
| §8 `SUPERSEDED`／`DEGRADED` 不污染 | 專項測試 | 通過 |
| §8 `Market.TPEX.value == "TPEx"` | 專項測試（含 `!= "TPEX"`） | 通過 |
| §9 七值 `ErrorCode` | 逐字比對 + `len == 7` | 通過 |
| §9.2 defensive copy、`str()` 不洩 context、`to_dict()` | 專項測試 | 通過 |
| §9.2 密鑰拒絕（大小寫、`api_key` 變體、巢狀） | 16 個根層 + 4 個巢狀參數化案例 | 通過 |
| §10.2 十三欄位型別與 Null 性 | model 定義 + 拒絕測試 | 通過 |
| §10.3 `available_from` **不得用 `max()`** | `test_available_from_selects_by_mode_not_max` 與 `test_max_merged_available_from_rejected_in_public_mode` | 通過 |
| §10.3 naive 拒絕、UTC 正規化為 Asia/Taipei | 7 欄位參數化 + 專項測試 | 通過 |
| §10.4 `__all__` 明確 export、不含私有 helper | 目視 + import 測試 | 通過 |

---

## 四、Git 證據

| 項目 | 值 |
|---|---|
| Branch | `feature/b0-skeleton` |
| **R02 closure commit** | `72d3cc059b41e7f59e42e57c5cb123a12fc3646e` |
| R03 base（`R03_BASE`） | 同上 |
| 起始 `git status --short` | 空（工作區乾淨） |
| **Head commit** | **uncommitted**（§13 明訂 R03 須待下一份 PASS 檢查報告後才收 commit） |
| `git diff --numstat` | 空——**無任何已追蹤檔被修改** |
| `git diff --check` | exit 0 |
| 未追蹤檔 | 8 個，全在 §7.2 授權清單 |

**R02 closure commit 執行紀錄：**

```console
$ git diff --cached --name-only | wc -l
17
$ git diff --cached --check
（無輸出）
$ ./scripts/check.sh          GATE_EXIT=0
$ git commit -F - ...
[feature/b0-skeleton 72d3cc0] chore: add quality gate and test skeleton
 17 files changed, 3491 insertions(+), 4 deletions(-)
$ git status --short
（空）
```

未執行 push、merge、rebase、amend、squash。

---

## 五、問題、偏差與風險

### B-1　【阻塞】mypy `[import-untyped]`——修法需要授權範圍外的檔案

**現象：** 完整 gate 停在 5/6，mypy 回報 4 個 `[import-untyped]`（§三 3.7）。

**根因：** 套件以 editable 安裝，但缺少 PEP 561 的 `src/hotstock/py.typed` 標記，mypy 因此把 `hotstock` 當成未提供型別的第三方庫。mypy 的錯誤訊息本身即指名 `missing library stubs or py.typed marker`。

**為什麼我不能自行解決：**

| 可能修法 | 阻礙 |
|---|---|
| 新增 `src/hotstock/py.typed` | **不在 §7.2 授權清單**（§7.2：「除上述檔案外，不得修改其他檔」） |
| `pyproject.toml` 加 `mypy_path`／`explicit_package_bases` | **§7.3 明訂不得修改 `pyproject.toml`** |
| 加 `# type: ignore[import-untyped]` | 等同以 ignore 換綠燈，違反 R02 §7.5 既有精神 |
| 改用相對 import | **已實測，但會使 strict override 失效**，見 V-2 |

觸發檢查報告 §14 停止條件第 7 項：「完整 gate 失敗，且原因不是本輪授權檔內可直接修正的實作錯誤」。

**選項與建議見 §六 Q-1。**

### V-2　相對 import 方案：已實測，**主動否決**

我測試過把 `models.py` 與 `__init__.py` 的絕對 import 改為相對 import（`from .enums import PitMode`）。**mypy 確實變成 exit 0。**

但清除 `.mypy_cache` 後對照兩種寫法的 `unused section(s)`：

| 寫法 | mypy exit | `unused section(s)` |
|---|:---:|---|
| 絕對 import（現況） | 1 | `['hotstock.data', 'hotstock.data.*']` |
| 相對 import | 0 | `['hotstock.data', 'hotstock.data.*', **'hotstock.domain', 'hotstock.domain.*'**]` |

**相對 import 讓 `hotstock.domain` 也進入 unused 清單，代表 §7.5 的 strict override 不再套用到本輪模組。** 它讓 gate 變綠的方式，是讓模組不再被識別為 `hotstock.domain.*`，等於悄悄關掉 strict 檢查——這比失敗更糟。

因此我**已還原為絕對 import**，並以此方案作為否決紀錄而非交付。§三 3.7 的輸出即為還原後的狀態。

> 首次量測時兩次執行結果不一致（`hotstock.domain` 一次在、一次不在 unused 清單），疑似 mypy cache 影響。上表是**清除 `.mypy_cache` 後連續兩次量測的穩定結果**。

### V-3　六個 RUF001／RUF002：以改寫自己的文字解決，未動白名單

首次 lint 出現 6 個錯誤，來自 `；`（U+FF1B）與 `／`（U+FF0F）。這兩者**不在核准的四字元白名單**，而 R02 檢查報告 §2.1 明文禁止加入未實際觀測的字元。

我的處置是**改寫本輪新檔中的文字**（`；`→`。`、`／`→`與`／`或`），全部落在授權檔內：

| 檔案 | 修改 |
|---|---|
| `domain/__init__.py:7` | `與 Label；` → `與 Label。` |
| `domain/errors.py:4` | `兩件事；retry` → `兩件事。retry` |
| `domain/errors.py:80` | `NaN／Infinity` → `NaN 或 Infinity` |
| `test_enums.py:140` | `執行階段；降級` → `執行階段。降級` |
| `test_errors.py:65` | `list／dict` → `list 與 dict` |
| `test_pit_metadata.py:74` | `18:30；正確` → `18:30。正確` |

> 附帶觀察：`。`（U+3002）與 `、`（U+3001）**不會**觸發 RUF00x，因為它們不是 ASCII 字元的全形對應，不屬 confusable。會觸發的只有 `，：（）；／` 這類全形 ASCII 對應字元。

### V-4　工時：估 3h48m，實際約 75 分鐘

| 輪次 | 估計 | 實際 | 比值 |
|---|---:|---:|---:|
| R00 | 3h | 1.5h | 0.50 |
| R01 | 3h25m | 1h | 0.29 |
| R02 | 3h35m | 1h10m | 0.33 |
| R02-FIX1 | 88m | 35m | 0.40 |
| R02-FIX2 | 35m | 15m | 0.43 |
| **R03** | **3h48m** | **約 75m** | **0.33** |

本輪雖已依承諾直接填寫下修後估計，仍高估約 3 倍。原因是我把「規格已完全寫死（§8–§11 逐欄位指定）」的實作，仍按需自行設計的粒度估算。

### V-5　`tests/unit/domain/__init__.py` 未建立（規劃 R-6 的處置）

§7.2 註明「只有測試匯入確實需要時才建立」。pytest 可正常 collect 三個測試檔（122 passed），故省略。

### 範圍外變更

**無。** 8 個新增檔全部在 §7.2 授權清單內；無任何已追蹤檔被修改。

---

## 六、待決事項

### Q-1　【阻塞】如何讓 mypy 正確分析 `hotstock` package

**背景：** 缺少 PEP 561 的 `py.typed` 標記，mypy 把本專案自己的 package 當成未提供型別的第三方庫（§三 3.7）。

| 選項 | 作法 | 評估 |
|---|---|---|
| **A（建議）** | 新增空檔 `src/hotstock/py.typed` | PEP 561 標準作法，一個空檔即可。Hatchling 預設會把 package 目錄內的檔案納入 wheel，無須改 `pyproject.toml`。**保留 strict override 正常運作。唯一阻礙是該檔不在 §7.2 授權清單，需要明確授權** |
| B | `pyproject.toml` 加 `mypy_path = "src"` 或 `explicit_package_bases` | **§7.3 明訂不得修改 `pyproject.toml`**；且僅解決 mypy，未解決套件對外的型別宣告 |
| C | 改用相對 import | **已實測並否決**——會讓 strict override 失效（§五 V-2） |
| D | 加 `# type: ignore[import-untyped]` | 以 ignore 換綠燈，違反既有品質精神 |

**我的建議是 A。** 它是 PEP 561 的標準解法、影響最小、且是唯一能同時「讓 gate 通過」與「維持 strict override 生效」的方案。

**但新增 `src/hotstock/py.typed` 超出 §7.2 的授權範圍，我不自行建立。** 請審查者裁示是否授權。

若裁示為 A，我預期只需新增該空檔並重跑 gate，不需改動本輪任何既有程式碼。

**在取得裁示前，R03 無法達成 §15 的「targeted 與完整 `check.sh` 全綠」條件。**

---

## 七、給審查者的重現步驟

```bash
cd ~/projects/hotstock-tw

# 1. 確認 base 與未提交狀態
git log -1 --oneline                    # 72d3cc0 chore: add quality gate...
git status --short                       # 僅 8 個未追蹤檔
git diff --numstat                       # 空

# 2. 重現阻塞（預期 exit 1，停在 5/6）
./scripts/check.sh; echo "GATE_EXIT=$?"

# 3. 確認唯一失敗原因
rm -rf .mypy_cache
uv run --frozen mypy src/hotstock/domain; echo "exit=$?"
uv run --frozen mypy src/hotstock/domain 2>&1 | rg -o '\[[a-z-]+\]' | sort | uniq -c
ls -la src/hotstock/py.typed             # 預期 No such file

# 4. 確認其餘步驟本身通過
uv lock --check
bash -n scripts/check.sh
uv run --frozen ruff format --check src/hotstock/domain tests/unit/domain
uv run --frozen ruff check src/hotstock/domain tests/unit/domain
uv run --frozen pytest tests/unit/domain -q          # 122 passed

# 5. 分項測試數
uv run --frozen pytest tests/unit/domain/test_enums.py -q         # 53
uv run --frozen pytest tests/unit/domain/test_errors.py -q        # 44
uv run --frozen pytest tests/unit/domain/test_pit_metadata.py -q  # 25

# 6. 驗證 V-2 的否決理由（相對 import 會讓 override 失效）
rm -rf .mypy_cache
uv run --frozen mypy src/hotstock/domain 2>&1 | rg 'unused section'
#   預期：['hotstock.data', 'hotstock.data.*']   ← hotstock.domain 不在其中

# 7. 範圍
git diff --name-only 72d3cc0             # 空
git ls-files --others --exclude-standard # 8 個授權檔
ls src/hotstock/data src/hotstock/adapters src/hotstock/cli.py 2>&1   # No such file
git diff --check

# 8. 本報告 checklist 計數
sed -n '/^## 一、Checklist/,/^\*\*計數/p' docs/工作報告/007_2026-08-02_B0-R03共用enum錯誤與PIT基礎.md | rg -c '^- \[x\]'    # 26
sed -n '/^## 一、Checklist/,/^\*\*計數/p' docs/工作報告/007_2026-08-02_B0-R03共用enum錯誤與PIT基礎.md | rg -c '^- \[[x ]\]' # 28

# 9. marker 收尾
tail -c 32 docs/工作報告/007_2026-08-02_B0-R03共用enum錯誤與PIT基礎.md | od -An -tx1
```

**預期：** 步驟 2 exit 1；步驟 3 顯示 4 × `[import-untyped]` 且 `py.typed` 不存在；步驟 4、5 全部 exit 0；步驟 7 前兩條分別為空與 8 個檔；步驟 8 依序 26／28；步驟 9 以 `2d 2d 3e 0a` 結尾。

---

## 八、停止聲明

**B0-R03 未完成，狀態 BLOCKED，工作已停止。**

- 阻塞於 §六 Q-1，**需審查者裁示是否授權新增 `src/hotstock/py.typed`**。
- **未自行採用任何未授權方案**——未建立 `py.typed`、未改 `pyproject.toml`、未加 `# type: ignore`、未保留相對 import（已還原）。
- 程式與測試本身已完成：12 個 enum、七類 error、`PitMetadata` 與 **122 個通過的單元測試**；Ruff format 與 lint 皆 exit 0。
- **完整 gate 未通過**，停在 5/6，第 6 段測試在 gate 內未被執行。我不以「測試單獨跑得過」宣稱 gate 通過。
- **未 commit。** §13 明訂 R03 須待下一份 PASS 檢查報告後才收 commit。
- 未 push、merge、rebase、amend、squash。
- **B0-R04 尚未開始。** `src/hotstock/data`、`src/hotstock/adapters`、`src/hotstock/cli.py` 均不存在。
- 等待 `docs/reviews/member-b/` 中新的檢查報告。

<!-- REPORT-COMPLETE -->
